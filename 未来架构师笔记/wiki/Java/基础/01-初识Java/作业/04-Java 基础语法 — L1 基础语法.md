---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---

> 课程：Java 语言基础入门 · 覆盖第 2 章全部 5 节  
> 说明：操作与定义题

---

## 1. 一、标识符与命名

### 1.1 （填空）Java 标识符不能以 **\_\_** 开头，也不能使用 **\_\_**。

### 1.2 （判断）文件名  `Demo.java`  中可以定义  `public class demo`。（ ）

### 1.3 （选择）下列合法标识符是：

- A. `2score`
- B. `_count`
- C. `class`
- D. `student-name`

### 1.4 （填空）公共类名习惯使用 **\_\_** 命名（帕斯卡/驼峰），普通变量习惯使用 **\_\_** 命名。

### 1.5 （填空）用  `final`  修饰的变量，赋值次数最多为 **\_\_** 次。

---

## 2. 二、基本数据类型

### 2.1 （填空）Java 是 **\_\_** 类型语言：变量声明时就要写明类型（如  `int age`）；类型在 **\_\_** 期检查。

### 2.2 （选择）下列关于成员变量与局部变量的说法，正确的是：

- A. 成员变量在方法内声明，没有默认值
- B. 局部变量在类里、方法外声明，有默认值 0
- C. 成员变量在类里、方法外声明，有默认值；局部变量在方法或块内声明，使用前必须赋值
- D. `main`  方法的参数  `args`  属于成员变量

### 2.3 （填空）Java 基本数据类型共有 **\_\_** 种；表示小数且默认可用、精度较高的是 **\_\_**。

### 2.4 （判断）`String`  是 Java 八种基本数据类型之一。（ ）

### 2.5 （填空）整数字面量  `3000000000`  超出  `int`  范围时，应写作 **\_\_**（带后缀）。

### 2.6 （填空）方法内的局部变量  `int x;`  在未赋值前能否使用？**\_\_**（能/不能）。

### 2.7 （选择）下列赋值正确的是：

- A. `char c = "A";`
- B. `float f = 3.14;`
- C. `long id = 20260001L;`
- D. `boolean ok = 1;`

---

## 3. 三、字面量、常量与作用域

### 3.1 （填空）字符字面量使用 **\_\_** 引号，字符串字面量使用 **\_\_** 引号。

### 3.2 （判断）`if`  代码块内声明的变量，可以在  `if`  块外继续使用。（ ）

### 3.3 （改错）下列代码有何问题？写出一句话原因。

```java
final int MAX = 100;
MAX = 200;
System.out.println(MAX);
```

---

## 4. 四、运算符

### 4.1 （填空）表达式  `7 / 2`  的结果是 **\_\_**；`7 / 2.0`  的结果是 **\_\_**。

### 4.2 （填空）逻辑运算符  `&&`  具有 **\_\_** 特性：左操作数为  `false`  时，右操作数可能不计算。

### 4.3 （读代码）执行后  `i`  的值是多少？

```java
int i = 5;
int a = i++;
int b = ++i;
```

### 4.4 （判断）`if (x = 1)`  在 Java 中是合法的条件表达式。（ ）

---

## 5. 五、类型转换

### 5.1 （填空）`byte`  与  `byte`  相加，表达式结果的类型是 **\_\_**。

### 5.2 （填空）将  `double`  变量  `d = 9.8`  赋给  `int`  变量，写法为：`int n = ______ d;`

### 5.3 （选择）`(int) 3.9`  的结果是：

- A. 4
- B. 3
- C. 3.9
- D. 编译错误

### 5.4 （简答）`char c = 'Z';`  执行  `char next = c + 1;`  能否编译通过？若不能，应如何修改（写一行）？

### 5.5 （读代码）`System.out.println(1 + '1');`  输出是多少？为什么不是  `11`？

### 5.6 （选择）下列表达式结果为字符串  `"11"`  的是：

- A. `1 + '1'`
- B. `1 + 1`
- C. `"1" + '1'`
- D. `'1' + '1'`

### 5.7 （判断）在 Java 中，表达式  `1 + '1'`  会把整数  `1`  自动转换成字符串  `"1"`  再与字符拼接。（ ）

### 5.8 （填空）当  `+`  两侧**都没有** `String`  时，`+`  表示 **\_\_** 运算，此时  `char`  会提升为 **\_\_** 参与计算；若要拼接文本，必须让 **\_\_** 类型参与表达式。

### 5.9 （填空）字符字面量用 **\_\_** 引号，字符串字面量用 **\_\_** 引号；`"A"`  的类型是 **\_\_**，`'A'`  的类型是 **\_\_**。

---

> 参考答案见  [[02-Java 基础语法 — 参考答案]]
