---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---
> 课程：Java 语言基础入门 · 面试与工作场景

---

## 1. [面试] 什么是静态类型、动态类型？Java 属于哪一种？

---

## 2. [面试] 什么是强类型？与 JavaScript 的弱类型相比，写 Java 时有什么好处？

---

## 3. [面试] 成员变量和局部变量有何区别？各自是否有默认值？

---

## 4. [面试] Java 中基本类型与引用类型有何区别？各举两个例子。

---

## 5. [面试] 说明 \`==\` 与 \`=\` 在 Java 中的不同；为什么 `if` 条件里不能写 `if (x = 1)`？

---

## 6. [面试] 什么是运算符短路求值？`&&` 与 `&` 用在布尔条件时有何区别？

---

## 7. [面试·扩展] 表达式 `5 / 2` 在 Java 与 Python 3 中结果可能不同，原因是什么？Java 中如何得到 `2.5`？

---

## 8. [面试] 自动类型提升（widening）与强制类型转换（narrowing）有何区别？各举一例。

---

## 9. [工作] 某开发者从 Python 转到 Java，写了如下代码编译失败：

```java
public class Demo {
    public static void main(String[] args) {
        int score;
        if (score >= 60) {
            System.out.println("及格");
        }
    }
}
```

请列出 **至少两条** 需要向该同事解释的差异（Java vs 动态语言习惯），并给出修改后的可编译片段（保持业务含义：有分数且 ≥60 输出及格）。

---

> 参考答案见 [[02-Java 基础语法 — 参考答案]]
