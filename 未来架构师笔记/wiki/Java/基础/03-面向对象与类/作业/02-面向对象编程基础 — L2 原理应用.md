---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---
> 课程：Java 面向对象与核心类库 · 第 1 章  
> 说明：根据现象推理结论，答案写清步骤

---

## 1.

某项目在 `StudentDemo` 中写了：

```java
Student s1 = new Student();
Student s2 = s1;
s1.setName("张三");
System.out.println(s2.getName());
```

控制台输出是什么？若希望 `s2` 改名不影响 `s1`，应如何改代码？（写出关键一行。）

---

## 2.

`User` 类仅有：

```java
public User(String name) {
    this.name = name;
}
```

另一文件的 `main` 中有 `User u = new User();`。编译报错，请按 **现象 → 原因 → 修复** 说明，并给出一种合法调用写法。

---

## 3.

`Book` 类定义了有参构造 `public Book(String title, double price)`，未写无参构造。同事新增：

```java
Book b = new Book();
```

IDE 报红。请说明编译器不再提供哪种构造，以及两种修复思路（各一句话）。

---

## 4.

运行 `InitOrderDemo` 的 `main` 创建对象时，日志顺序与「成员默认值 → 实例块 → 构造器」是否一致？若 `x` 在实例块打印为 0、构造器打印仍为 0 之后才在构造器末尾变为 10，这说明了什么？

---

## 5.

`Employee` 的父类 `Person` 只有 `public Person(String name)`。子类构造器写成：

```java
public Employee(String name, String dept) {
    this.dept = dept;
    super(name);
}
```

无法通过编译。请说明 **首行规则** 与 **父类构造** 两条原因，并写出修正后的构造器前两行（保持参数不变）。

---

## 6.

`BankAccount` 将 `balance` 设为 `private`，但未提供 `deposit`/`getBalance`，外部 `main` 无法读写余额。这是封装过度还是缺少对外接口？应补什么（列举至少一种方法名）？

---

## 7.

`Book` 中无参构造为：

```java
public Book() {
    this("未命名", 0.0);
}
```

两参构造为：

```java
public Book(String title, double price) {
    this.title = title;
    this.price = price;
}
```

若误写成 `public Book() { this(); }`，会出现什么问题？（编译期还是运行期？一句话。）

---

## 8.

同包类 `A` 中有成员 `int count;`（无修饰符）。同包 `B` 可访问 `a.count`，另一包 `C` 不能。这对应哪种访问级别？与 `private`、`public` 相比，用一句话概括可见范围。
