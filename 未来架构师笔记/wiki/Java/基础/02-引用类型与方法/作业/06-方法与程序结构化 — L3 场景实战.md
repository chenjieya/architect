---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---

> 课程：Java 语言基础入门 · 面试与工作场景

---

## 1. [面试] Java 是值传递还是引用传递

请说明 Java 方法参数传递的准确说法；基本类型与引用类型实参分别传递的是什么？为何说「数组是引用传递」容易造成误解？

---

## 2. [面试] 形参 reassign 与改数组元素

方法接收  `int[] arr`  时，下列两种操作对调用方有何不同？

1. `arr[0] = 100;`
2. `arr = new int[5];`

请各用一句话说明调用方能否看到变化，以及原因。

---

## 3. [面试] 方法重载规则

要定义两个都叫  `parse`  的方法，一个返回  `int`、一个返回  `double`，参数都是  `String s`，是否合法？为什么？合法的重载至少应满足什么条件？

---

## 4. [面试] 递归两要素

写递归方法时，必须明确哪两个部分？若缺少第一部分，运行期可能出现什么错误？

---

## 5. [面试] 递归与迭代

以阶乘为例，说明递归版和  `for`  循环版各有什么特点；简单累乘在工程中更常先用哪一种？（不要求写完整代码）

---

## 6. [面试] 固定参数、可变参数与默认参数

请说明 Java 方法参数列表的三种相关概念：**固定形参**、**可变参数（varargs）**、**默认参数值**。Java 分别是否支持？少传参、传不定个数同类型实参时，本课推荐用什么方式？

---

## 7. [面试·扩展] void swap 两个 int

```java
public static void swap(int a, int b) {
    int t = a;
    a = b;
    b = t;
}
```

调用  `swap(x, y)`  后，`main`  里的  `x`、`y`  会交换吗？请结合传递机制说明；若本课范围内要「交换两个 int 的值」，可以提一种思路（如传入  `int[]`  存两个数），不要求写完整实现。

---

## 8. [工作] 控制台录入无效分数

`ScoreMenuDemo`  中用户输入  `"abc"`  或  `"150"`  时应提示「输入无效」而不崩溃。请说明：

1. 本课示例用哪种方式读入（`nextLine`  还是  `nextInt`）更稳？
2. `isValidScoreText`  至少应检查哪几类问题（列举三项即可）？
3. 数组追加成绩时，为何必须写  `scores = appendScore(scores, score)`？

---

> 参考答案见  [[02-方法与程序结构化 — 参考答案]]
