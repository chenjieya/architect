---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---

## 1. 课程目标

- 理解 Java 中的常用容器类型，能够基于需求选择合适的容器
- 掌握针对 Java 容器的通用操作
- 掌握特定容器的结构和使用方式

## 2. 什么是容器类型？

容器类型用于**存储多个数据**。Java 中的容器主要分为**数组**和**集合框架（Collection Framework）**  两大类。集合框架提供了丰富的接口和实现类，常用的包括：

| 接口/类型 | 实现类                     | 是否可变         | 是否有序                                     | 元素是否可重复 | 示例             |
| --------- | -------------------------- | ---------------- | -------------------------------------------- | -------------- | ---------------- |
| `List`    | `ArrayList`, `LinkedList`  | 可变             | 有序（按插入顺序）                           | 可重复         | `[1, 2, 2, 3]`   |
| `Set`     | `HashSet`, `TreeSet`       | 可变             | `HashSet`  无序，`TreeSet`  有序（自然顺序） | 不可重复       | `{1, 2, 3}`      |
| `Map`     | `HashMap`, `TreeMap`       | 可变             | `HashMap`  无序，`TreeMap`  有序（按键排序） | 键不可重复     | `{"a":1, "b":2}` |
| `Queue`   | `ArrayDeque`, `LinkedList` | 可变             | 有序（FIFO 或优先级）                        | 可重复         | `[1, 2, 3]`      |
| 数组      | `int[]`, `String[]`  等    | 可变（长度固定） | 有序                                         | 可重复         | `{1, 2, 3}`      |
| `String`  | `String`（字符序列）       | 不可变           | 有序                                         | 可重复         | `"hello"`        |

> [!CAUTION]
>
> 数组长度固定，集合类长度动态可变。`String`  可以看作是不可变的字符序列，也支持索引、切片（通过  `substring`）等操作。

> [!CAUTION]
>
> 在 Java 中，容器往往与`泛型`一起使用，用于该容器中所保存的数据类型。关于泛型我们会在第 6 讲中具体展开，泛型的基础语法：List nums = new ArrayList<>();

---

## 3. List 接口（列表）

`List`  是**可变、有序**的集合，允许重复元素，通过索引访问。常用实现类：`ArrayList`（基于数组，随机访问快）和  `LinkedList`（基于链表，插入删除快）。

### 3.1 创建 List

```java
import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;
import java.util.Arrays;

// 使用 ArrayList
List<Integer> nums = new ArrayList<>();            // 空列表
List<String> fruits = new ArrayList<>(Arrays.asList("苹果", "香蕉", "橙子"));

// 使用 LinkedList
List<String> linked = new LinkedList<>();

// 通过 Arrays.asList 快速初始化
List<Integer> list = Arrays.asList(1, 2, 3, 4, 5); // 返回固定大小列表（不可增删，但可修改元素）
```

### 3.2 索引与切片（子列表）

```java
List<String> fruits = new ArrayList<>(Arrays.asList("苹果", "香蕉", "橙子", "葡萄", "西瓜"));

// 索引访问（从0开始）
System.out.println(fruits.get(0));   // 苹果
System.out.println(fruits.get(fruits.size() - 1)); // 西瓜

// 子列表（类似切片）[start, end) 不包括 end
List<String> sub = fruits.subList(1, 4); // [香蕉, 橙子, 葡萄]
System.out.println(sub);

// 注意：subList 是原列表的视图，修改会影响原列表
sub.set(0, "芒果");
System.out.println(fruits); // [苹果, 芒果, 橙子, 葡萄, 西瓜]
```

### 3.3 List 操作

```java
List<Integer> nums = new ArrayList<>(Arrays.asList(1, 2, 3));

// 添加元素
nums.add(4);                 // [1, 2, 3, 4] 末尾添加
nums.add(0, 0);              // [0, 1, 2, 3, 4] 指定位置插入
nums.addAll(Arrays.asList(5, 6)); // [0,1,2,3,4,5,6] 批量添加

// 删除元素
nums.remove(Integer.valueOf(0)); // [1,2,3,4,5,6] 删除第一个匹配值（按对象）
nums.remove(0);                  // [2,3,4,5,6] 删除指定索引
Integer popped = nums.remove(nums.size() - 1); // 6, nums变为[2,3,4,5]
nums.clear();                    // [] 清空

// 查找与统计
nums = new ArrayList<>(Arrays.asList(1, 2, 3, 2, 4));
System.out.println(nums.indexOf(2));    // 1 第一个匹配的位置
System.out.println(nums.lastIndexOf(2));// 3 最后一个匹配的位置
System.out.println(Collections.frequency(nums, 2)); // 2 出现次数

// 排序与反转
Collections.sort(nums);                 // [1, 2, 2, 3, 4] 升序
Collections.sort(nums, Collections.reverseOrder()); // [4,3,2,2,1] 降序
Collections.reverse(nums);              // [1,2,2,3,4] 反转

// 修改元素
nums.set(0, 10); // [10, 2, 2, 3, 4]
```

---

## 4. Set 接口（集合）

`Set`  是**无序（或有序）**  的集合，**不允许重复元素**。常用实现类：`HashSet`（哈希表，无序）、`TreeSet`（红黑树，按自然顺序或比较器排序）。

### 4.1 创建 Set

```java
import java.util.HashSet;
import java.util.TreeSet;
import java.util.Set;

// HashSet
Set<Integer> nums = new HashSet<>();
Set<String> chars = new HashSet<>(Arrays.asList("h", "e", "l", "l", "o")); // 自动去重

// TreeSet（有序）
Set<Integer> sorted = new TreeSet<>(Arrays.asList(3, 1, 4, 1, 5));
System.out.println(sorted); // [1, 3, 4, 5]（自动排序+去重）
```

### 4.2 Set 操作

```java
Set<Integer> s = new HashSet<>(Arrays.asList(1, 2, 3));

// 添加/删除
s.add(4);          // {1,2,3,4}
s.remove(2);       // {1,3,4} 不存在则无效果
s.remove(10);      // 无效果，不报错
s.clear();         // {}

// 去重利器
List<Integer> list = Arrays.asList(1, 2, 2, 3, 3, 3);
Set<Integer> unique = new HashSet<>(list); // {1,2,3}（顺序不确定）
```

### 4.3 集合运算（交集、并集、差集）

```java
Set<Integer> s1 = new HashSet<>(Arrays.asList(1, 2, 3));
Set<Integer> s2 = new HashSet<>(Arrays.asList(2, 3, 4));

// 交集（保留 s1 中同时也存在于 s2 的元素）
Set<Integer> intersection = new HashSet<>(s1);
intersection.retainAll(s2); // {2, 3}

// 并集
Set<Integer> union = new HashSet<>(s1);
union.addAll(s2); // {1, 2, 3, 4}

// 差集（s1 - s2）
Set<Integer> diff = new HashSet<>(s1);
diff.removeAll(s2); // {1}
```

---

## 5. Map 接口（字典，映射表，键值对）

`Map`  存储**键值对**，键不可重复，值可重复。常用实现类：`HashMap`（无序）、`TreeMap`（键有序）。

### 5.1 创建 Map

```java
import java.util.HashMap;
import java.util.TreeMap;
import java.util.Map;

// HashMap
Map<String, Integer> person = new HashMap<>();
person.put("age", 40); // 注意：Map 中键和值都是对象，这里用 String 和 Integer

// 也可以使用 Map.of 创建不可变Map（Java 9+）
Map<String, Integer> immutable = Map.of("age", 25, "score", 90);

// TreeMap（键有序）
Map<Integer, String> sortedMap = new TreeMap<>();
sortedMap.put(3, "three");
sortedMap.put(1, "one");
sortedMap.put(2, "two");
System.out.println(sortedMap); // {1=one, 2=two, 3=three}
```

### 5.2 访问与修改

```java
Map<String, Object> person = new HashMap<>();
person.put("name", "Alice");
person.put("age", 25);

// 访问
System.out.println(person.get("name")); // Alice
System.out.println(person.get("gender")); // null（不存在返回null）

// 安全访问（提供默认值）
String gender = (String) person.getOrDefault("gender", "未知");
System.out.println(gender); // 未知

// 添加/修改
person.put("gender", "女");    // 添加新键
person.put("age", 26);         // 修改已有键

// 批量更新
Map<String, Object> extra = new HashMap<>();
extra.put("phone", "123456");
extra.put("age", 27);
person.putAll(extra);

// 删除
person.remove("phone");        // 删除键值对
Object value = person.remove("age"); // 删除并返回值
person.clear();                // 清空
```

---

## 6. 数组（Array）

数组是 Java 中最基础的容器，长度固定，元素类型统一，支持索引访问。

### 6.1 创建数组

```java
// 声明并初始化
int[] nums = {1, 2, 3, 4, 5};
String[] fruits = {"苹果", "香蕉", "橙子"};

// 使用 new 创建
int[] arr = new int[5];         // 默认元素为0
arr[0] = 10;

// 匿名数组
printArray(new int[]{1, 2, 3});
```

### 6.2 数组操作（借助  `Arrays`  工具类）

```java
import java.util.Arrays;

int[] nums = {3, 1, 4, 1, 5};

// 排序
Arrays.sort(nums);              // [1, 1, 3, 4, 5]

// 查找（必须先排序）
int index = Arrays.binarySearch(nums, 4); // 3

// 复制
int[] copy = Arrays.copyOf(nums, nums.length);

// 转字符串
System.out.println(Arrays.toString(nums)); // [1, 1, 3, 4, 5]

// 比较内容
int[] a = {1, 2, 3};
int[] b = {1, 2, 3};
System.out.println(Arrays.equals(a, b)); // true
```

> [!CAUTION]
>
> 数组长度固定，不能动态增减。如需动态容器，使用  `ArrayList`  等集合类。

---

## 7. String 作为字符序列

`String`  是不可变的字符序列，可视为有序容器，支持索引、切片、遍历等操作。

### 7.1 创建与操作

```java
String s = "hello";

// 索引访问（字符）
System.out.println(s.charAt(0));   // h
System.out.println(s.charAt(s.length()-1)); // o

// 切片（子串）[start, end)
System.out.println(s.substring(1, 4)); // ell

// 包含判断
System.out.println(s.contains("el"));   // true
System.out.println(s.indexOf("x"));     // -1

// 连接与重复
String s2 = s.concat(" world"); // "hello world"
String s3 = s.repeat(2);        // "hellohello" (Java 11+)

// 遍历字符
for (char c : s.toCharArray()) {
    System.out.print(c + " ");  // h e l l o
}
```

---

## 8. 通用操作

以下方法适用于所有集合类（`List`、`Set`、`Map`  等，数组使用  `Arrays`  工具类）。

### 8.1 集合通用方法

```java
List<Integer> list = Arrays.asList(1, 2, 3, 4);
Set<Integer> set = new HashSet<>(list);
Map<String, Integer> map = Map.of("a", 1, "b", 2);

// 大小
list.size();   // 4
set.size();    // 4
map.size();    // 2

// 是否为空
list.isEmpty(); // false

// 包含（Map 检查键）
list.contains(3); // true
set.contains(3);  // true
map.containsKey("a"); // true
map.containsValue(2); // true

// 转数组
Integer[] arr = list.toArray(new Integer[0]);

// 迭代器
Iterator<Integer> it = list.iterator();
while (it.hasNext()) {
    System.out.println(it.next());
}
```

### 8.2 数组通用操作（`Arrays`  类）

```java
int[] arr = {3, 1, 4, 1, 5};

// 排序
Arrays.sort(arr);

// 二分查找
int idx = Arrays.binarySearch(arr, 4); // 3

// 填充
Arrays.fill(arr, 0); // 全部置0

// 转字符串
System.out.println(Arrays.toString(arr));
```

---

## 9. 常用运算符与操作

### 9.1 成员检查：`contains` / `containsKey` / `containsValue`

```java
List<String> list = Arrays.asList("a", "b", "c");
list.contains("b");   // true

Set<Integer> set = new HashSet<>(Arrays.asList(1,2,3));
set.contains(2);      // true

Map<String, Integer> map = new HashMap<>();
map.put("key", 123);
map.containsKey("key"); // true
map.containsValue(123); // true
```

### 9.2 连接与重复（集合）

Java 不支持  `+`  或  `*`  运算符直接操作集合，需使用方法：

```java
// 列表连接
List<Integer> list1 = new ArrayList<>(Arrays.asList(1, 2));
List<Integer> list2 = Arrays.asList(3, 4);
list1.addAll(list2); // [1, 2, 3, 4]

// 字符串重复（Java 11+）
String s = "ab".repeat(3); // "ababab"
```

### 9.3 相等比较：`equals`  和  `==`

- `==`  比较引用（是否同一对象）
- `equals`  比较内容

```java
List<Integer> a = new ArrayList<>(Arrays.asList(1, 2));
List<Integer> b = new ArrayList<>(Arrays.asList(1, 2));
System.out.println(a == b);      // false（不同对象）
System.out.println(a.equals(b)); // true（内容相同）

// 数组使用 Arrays.equals
int[] arr1 = {1, 2};
int[] arr2 = {1, 2};
System.out.println(arr1 == arr2);      // false
System.out.println(Arrays.equals(arr1, arr2)); // true
```

### 9.4 集合运算（Set 专用）

```java
// 交集 retainAll, 并集 addAll, 差集 removeAll
Set<Integer> s1 = new HashSet<>(Arrays.asList(1,2,3));
Set<Integer> s2 = new HashSet<>(Arrays.asList(2,3,4));
s1.retainAll(s2); // s1 变为 {2,3}
```

---

## 10. 遍历容器

### 10.1 索引遍历（List / 数组）

```java
// List 索引遍历
List<String> list = Arrays.asList("a", "b", "c");
for (int i = 0; i < list.size(); i++) {
    System.out.println(list.get(i));
}

// 数组索引遍历
int[] arr = {10, 20, 30};
for (int i = 0; i < arr.length; i++) {
    System.out.println(arr[i]);
}
```

### 10.2 ✅ 增强 for 循环（for-each）

适用于所有  `Iterable`（集合）和数组。

```java
// 遍历 List
for (String s : list) {
    System.out.println(s);
}

// 遍历 Set
for (int num : set) {
    System.out.println(num);
}

// 遍历数组
for (int val : arr) {
    System.out.println(val);
}

// 遍历 Map 的键值对
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println(entry.getKey() + "=" + entry.getValue());
}
```

### 10.3 使用 Iterator（遍历过程中可删除元素）

```java
List<String> list = new ArrayList<>(Arrays.asList("a", "b", "c"));

Iterator<String> it = list.iterator();
while (it.hasNext()) {
    String item = it.next();
    if (item.equals("b")) {
        it.remove(); // 安全删除
    }
}
```

### 10.4 遍历 Map 的三种方式

```java
Map<String, Integer> map = new HashMap<>();
map.put("a", 1);
map.put("b", 2);

// 1. 遍历键
for (String key : map.keySet()) {
    System.out.println(key);
}

// 2. 遍历值
for (Integer val : map.values()) {
    System.out.println(val);
}

// 3. 遍历键值对
for (Map.Entry<String, Integer> entry : map.entrySet()) {
    System.out.println(entry.getKey() + ": " + entry.getValue());
}
```

## 11. 思维导图

![image.png](https://picgo-1300696809.cos.ap-beijing.myqcloud.com/20260719160528609.png)

---

## 12. 作业

### 12.1 作业一：List 与相等比较综合

阅读以下代码，预测每行  `System.out.println`  的输出结果，并在注释中写出你的答案。

```java
import java.util.*;

public class Homework1 {
    public static void main(String[] args) {
        List<Integer> nums = new ArrayList<>(Arrays.asList(3, 1, 4, 1, 5));
        List<Integer> copy = new ArrayList<>(nums);
        System.out.println(nums.equals(copy) + " " + (nums == copy));
        nums.set(0, 10);

        System.out.println(nums.get(0) + " " + copy.get(0));
        System.out.println(nums.equals(copy) + " " + (nums == copy));
        System.out.println(nums.size());

        int i = 0, count = 0;
        while (i < nums.size()) {
            if (nums.get(i) > 3) count++;
            i++;
        }
        System.out.println(count);
    }
}
```

### 12.2 作业二：Set 与集合运算综合

阅读以下代码，预测输出。

```java
import java.util.*;

public class Homework2 {
    public static void main(String[] args) {
        List<Integer> t = Arrays.asList(5, 2, 8, 2);
        Set<Integer> s = new HashSet<>(t);
        System.out.println(s.size());
        System.out.println(s.contains(2) + " " + !s.contains(9));

        Set<Integer> s2 = new HashSet<>(Arrays.asList(2, 8, 10));
        Set<Integer> inter = new HashSet<>(s);
        inter.retainAll(s2);
        Set<Integer> union = new HashSet<>(s);
        union.addAll(s2);
        System.out.println(inter + " " + union);
    }
}
```

### 12.3 作业三：Map 与遍历综合

阅读以下代码，预测输出。

```java
import java.util.*;

public class Homework3 {
    public static void main(String[] args) {
        Map<String, Integer> d = new HashMap<>();
        d.put("x", 10);
        d.put("y", 20);
        d.put("z", 30);
        d.put("x", 15);
        System.out.println(d.size());
        System.out.println(d.containsKey("x") + " " + d.containsValue(20));

        System.out.println(d.getOrDefault("w", 0));

        List<String> keys = new ArrayList<>();
        for (String k : d.keySet()) {
            keys.add(k);
        }
        System.out.println(keys);

        int i = 0;
        while (i < keys.size()) {
            String k = keys.get(i);
            if (d.get(k) > 15) {
                System.out.println(k);
            }
            i++;
        }
    }
}
```

### 12.4 作业四：String 操作与循环综合

阅读以下代码，预测输出。

```java
public class Homework4 {
    public static void main(String[] args) {
        String s = "hello";
        System.out.println(s.substring(1, 4));
        System.out.println(s.contains("el") + " " + !s.contains("x"));

        System.out.println(s.concat(" world") + " " + s.repeat(2));

        String vowels = "aeiou";
        int count = 0;
        for (int i = 0; i < s.length(); i++) {
            if (vowels.indexOf(s.charAt(i)) != -1) count++;
        }
        System.out.println(count);
        System.out.println("length: " + s.length());
    }
}
```

### 12.5 作业五：多容器遍历综合

阅读以下代码，预测输出。

```java
import java.util.*;

public class Homework5 {
    public static void main(String[] args) {
        List<String> names = Arrays.asList("Alice", "Bob");
        List<Integer> ages = Arrays.asList(25, 30);

        List<String> pairs = new ArrayList<>();
        for (int i = 0; i < names.size(); i++) {
            pairs.add(names.get(i) + "-" + ages.get(i));
        }
        System.out.println(pairs);

        // 模拟 enumerate
        int idx = 1;
        for (String name : names) {
            System.out.println(idx + " " + name);
            idx++;
        }

        Map<String, Integer> d = new HashMap<>();
        for (int i = 0; i < names.size(); i++) {
            if (ages.get(i) > 20) {
                d.put(names.get(i), ages.get(i));
            }
        }
        System.out.println(d.size());
        System.out.println(d.getOrDefault("Alice", "not found"));
        System.out.println(d.getOrDefault("Charlie", "not found"));
    }
}
```

### 12.6 作业六：图书信息管理综合练习

以下是一个包含图书信息的列表，每本书用一个  `Map<String, Object>`  表示（键包括  `id`、`title`、`author`、`price`、`year`、`category`  等）。请根据要求编写代码完成各项任务。

数据初始化（请复制到代码中）：

```java
import java.util.*;

/**
 * 作业六：图书信息管理综合练习
 */
public class Homework6 {
    public static void main(String[] args) {
        // ---------- 数据初始化 ----------
        List<Map<String, Object>> books = new ArrayList<>();

        // 直接添加每本书（不使用辅助方法）
        Map<String, Object> book1 = new HashMap<>();
        book1.put("id", 1001);
        book1.put("title", "Java核心技术");
        book1.put("author", "Cay S. Horstmann");
        book1.put("price", 89.0);
        book1.put("year", 2019);
        book1.put("category", "编程");
        books.add(book1);

        Map<String, Object> book2 = new HashMap<>();
        book2.put("id", 1002);
        book2.put("title", "三体");
        book2.put("author", "刘慈欣");
        book2.put("price", 93.0);
        book2.put("year", 2008);
        book2.put("category", "科幻");
        books.add(book2);

        Map<String, Object> book3 = new HashMap<>();
        book3.put("id", 1003);
        book3.put("title", "时间简史");
        book3.put("author", "霍金");
        book3.put("price", 45.0);
        book3.put("year", 2010);
        book3.put("category", "科学");
        books.add(book3);

        Map<String, Object> book4 = new HashMap<>();
        book4.put("id", 1004);
        book4.put("title", "Python编程从入门到实践");
        book4.put("author", "Eric Matthes");
        book4.put("price", 79.0);
        book4.put("year", 2016);
        book4.put("category", "编程");
        books.add(book4);

        Map<String, Object> book5 = new HashMap<>();
        book5.put("id", 1005);
        book5.put("title", "百年孤独");
        book5.put("author", "马尔克斯");
        book5.put("price", 55.0);
        book5.put("year", 2011);
        book5.put("category", "小说");
        books.add(book5);

        Map<String, Object> book6 = new HashMap<>();
        book6.put("id", 1006);
        book6.put("title", "算法导论");
        book6.put("author", "Thomas H. Cormen");
        book6.put("price", 128.0);
        book6.put("year", 2009);
        book6.put("category", "编程");
        books.add(book6);

        Map<String, Object> book7 = new HashMap<>();
        book7.put("id", 1007);
        book7.put("title", "人类简史");
        book7.put("author", "尤瓦尔·赫拉利");
        book7.put("price", 68.0);
        book7.put("year", 2014);
        book7.put("category", "历史");
        books.add(book7);

        Map<String, Object> book8 = new HashMap<>();
        book8.put("id", 1008);
        book8.put("title", "三体II：黑暗森林");
        book8.put("author", "刘慈欣");
        book8.put("price", 93.0);
        book8.put("year", 2008);
        book8.put("category", "科幻");
        books.add(book8);

        Map<String, Object> book9 = new HashMap<>();
        book9.put("id", 1009);
        book9.put("title", "三体III：死神永生");
        book9.put("author", "刘慈欣");
        book9.put("price", 93.0);
        book9.put("year", 2010);
        book9.put("category", "科幻");
        books.add(book9);

        Map<String, Object> book10 = new HashMap<>();
        book10.put("id", 1010);
        book10.put("title", "深入理解Java虚拟机");
        book10.put("author", "周志明");
        book10.put("price", 129.0);
        book10.put("year", 2019);
        book10.put("category", "编程");
        books.add(book10);

        Map<String, Object> book11 = new HashMap<>();
        book11.put("id", 1011);
        book11.put("title", "活着");
        book11.put("author", "余华");
        book11.put("price", 35.0);
        book11.put("year", 2012);
        book11.put("category", "小说");
        books.add(book11);

        Map<String, Object> book12 = new HashMap<>();
        book12.put("id", 1012);
        book12.put("title", "围城");
        book12.put("author", "钱钟书");
        book12.put("price", 39.0);
        book12.put("year", 2000);
        book12.put("category", "小说");
        books.add(book12);

        Map<String, Object> book13 = new HashMap<>();
        book13.put("id", 1013);
        book13.put("title", "万历十五年");
        book13.put("author", "黄仁宇");
        book13.put("price", 45.0);
        book13.put("year", 2006);
        book13.put("category", "历史");
        books.add(book13);

        Map<String, Object> book14 = new HashMap<>();
        book14.put("id", 1014);
        book14.put("title", "宇宙简史");
        book14.put("author", "霍金");
        book14.put("price", 38.0);
        book14.put("year", 2015);
        book14.put("category", "科学");
        books.add(book14);

        Map<String, Object> book15 = new HashMap<>();
        book15.put("id", 1015);
        book15.put("title", "数据结构与算法分析");
        book15.put("author", "Mark Allen Weiss");
        book15.put("price", 79.0);
        book15.put("year", 2012);
        book15.put("category", "编程");
        books.add(book15);

        Map<String, Object> book16 = new HashMap<>();
        book16.put("id", 1016);
        book16.put("title", "人性的弱点");
        book16.put("author", "戴尔·卡耐基");
        book16.put("price", 32.0);
        book16.put("year", 2018);
        book16.put("category", "心理");
        books.add(book16);

        Map<String, Object> book17 = new HashMap<>();
        book17.put("id", 1017);
        book17.put("title", "富爸爸穷爸爸");
        book17.put("author", "罗伯特·清崎");
        book17.put("price", 39.0);
        book17.put("year", 2000);
        book17.put("category", "经管");
        books.add(book17);

        Map<String, Object> book18 = new HashMap<>();
        book18.put("id", 1018);
        book18.put("title", "经济学原理");
        book18.put("author", "曼昆");
        book18.put("price", 88.0);
        book18.put("year", 2015);
        book18.put("category", "经管");
        books.add(book18);

        Map<String, Object> book19 = new HashMap<>();
        book19.put("id", 1019);
        book19.put("title", "香水");
        book19.put("author", "帕特里克·聚斯金德");
        book19.put("price", 42.0);
        book19.put("year", 2010);
        book19.put("category", "小说");
        books.add(book19);

        Map<String, Object> book20 = new HashMap<>();
        book20.put("id", 1020);
        book20.put("title", "我的前半生");
        book20.put("author", "溥仪");
        book20.put("price", 55.0);
        book20.put("year", 2007);
        book20.put("category", "历史");
        books.add(book20);

        Map<String, Object> book21 = new HashMap<>();
        book21.put("id", 1021);
        book21.put("title", "流浪地球");
        book21.put("author", "刘慈欣");
        book21.put("price", 35.0);
        book21.put("year", 2012);
        book21.put("category", "科幻");
        books.add(book21);

        Map<String, Object> book22 = new HashMap<>();
        book22.put("id", 1022);
        book22.put("title", "微积分学教程");
        book22.put("author", "菲赫金哥尔茨");
        book22.put("price", 150.0);
        book22.put("year", 2014);
        book22.put("category", "科学");
        books.add(book22);

        //TODO：添加具体任务
    }
}
```

**任务列表：**

1. 遍历  `books`  列表，使用  `System.out.printf`  格式化输出每本书的标题和价格，格式为  `《标题》 价格: ¥xx.xx`
2. 创建一个新列表  `programmingBooks`，包含所有类别为  `"编程"`  的图书
3. 创建一个新列表  `recentBooks`，包含所有出版年份晚于 2015 年的图书
4. 创建一个新列表  `cheapBooks`，包含所有价格低于 50 元的图书
5. 创建一个新列表  `authorBooks`，包含所有作者为  `"刘慈欣"`  的图书（可替换为任一常见作者）
6. 创建一个新列表  `titles`，包含所有图书的标题
7. 创建一个新列表  `expensiveTitles`，包含所有价格高于 80 元的图书的标题
8. 创建一个新列表  `bookSummaries`，其中每个元素是一个  `Map`，只包含  `title`  和  `price`  两个键，包含所有图书的信息
9. 计算并打印所有图书的总价格
10. 计算并打印所有图书的平均价格（保留两位小数，使用  `System.out.printf`）
11. 创建一个  `Map<String, List<String>>`  对象  `categoryMap`，键为图书类别，值为该类别的所有图书标题列表（例如  `"编程" -> ["Java核心技术", "Python入门"]`）
12. 找到  `id`  为  `1024`  的图书，打印其标题和作者。如果找不到，打印  `"未找到该图书"`
13. 判断是否存在价格大于 150 元的图书。如果存在，打印  `true`  并打印该图书的标题；否则打印  `false`
14. 判断是否所有图书的价格都低于 200 元。如果是，打印  `true`；否则打印  `false`
