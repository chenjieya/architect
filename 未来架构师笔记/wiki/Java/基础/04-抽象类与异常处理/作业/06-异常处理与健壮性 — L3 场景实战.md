---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---
> 课程：Java 面向对象与核心类库 · 第 3 章  
> 说明：面试题为主，少量工作场景

---

## 1. [面试]

Java 中 **受检异常** 与 **非受检异常** 的区别是什么？各举本章一个类名示例。

---

## 2. [面试]

`throw` 与 `throws` 分别用在什么位置、表达什么含义？

---

## 3. [面试]

`try-catch-finally` 中 **finally** 的作用是什么？`try-with-resources` 解决了什么问题？

---

## 4. [面试]

为什么要自定义 **`InsufficientFundsException`** 这类业务异常，而不是到处 `throw new RuntimeException("余额不足")`？

---

## 5. [面试·扩展]

Java 的受检异常设计常被认为「繁琐」。与 Python/JS 相比，编译期强制处理带来了什么好处与代价？（各列 1～2 点即可）

---

## 6. [面试]

`NullPointerException` 常见成因有哪些？排查时如何阅读 **Stack Trace**？（至少 3 步）

---

## 7. [工作]

线上日志出现 `NullPointerException at OrderDemo.main(OrderDemo.java:22)`，而业务说是「订单不存在」。从 **异常选型** 与 **返回值约定** 两方面，你会如何改进 `OrderService.findById` 的设计（列 2 条，不写框架代码）？

---

## 8. [面试]

多个 catch 块为何要求 **先子类后父类**？若 `catch (Exception e)` 写在第一个会有什么问题？

---

## 9. [面试]

`Error` 与 `Exception` 在 Throwable 体系中的区别是什么？业务代码通常应如何处理 `OutOfMemoryError`？

---

## 10. [工作]

`FileReadHelper.readFirstLine` 在多处被调用。有人建议在每个调用点空 `catch (IOException e) {}`。从 **异常传播** 与 **可维护性** 说明这种做法的问题，并给出一种更合理的分层处理思路（如记录日志 + 向上抛包装异常，一句话方向即可）。
