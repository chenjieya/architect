---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---

> 课程：Java 面向对象与核心类库 · 第 2 章  
> 说明：面试题为主，少量工作场景

---

## 1. [面试]

Java 中  **方法重写（Override）**  与  **方法重载（Overload）**  的区别是什么？重写时有哪些常见约束（至少列 3 条）？

---

## 2. [面试]

什么是  **多态**？请结合  `Shape s = new Circle(3.0); s.area();`  说明  **编译期类型**、**运行期类型**  与  **运行时绑定**  分别指什么。

---

## 3. [面试]

**抽象类**  与  **接口**  的主要区别是什么？各举一个本章示例说明适用场景（`AbstractReport` / `Payable`）。

---

## 4. [面试]

解释  **面向接口编程**  的含义。为什么业务代码里常写  `Payable payment`  而不是  `AlipayPayment payment`？

---

## 5. [面试·扩展]

有人说「继承是为了复用代码」。结合本章  **组合优于继承**，说明何时应优先用  **组合 + 委托**  而不是  `extends`（举  `Car`  与  `Engine`  或同类例子）。

---

## 6. [面试]

Java 为什么采用  **类单继承 + 接口多实现**？与 Python **多继承**  相比，设计上的主要取舍是什么？（点到即可，不展开 MRO。）

---

## 7. [工作]

你接手一个  `ReportService`，内部写死  `new PdfReport(...).generate()`，现在要支持 TXT 且可能还有 HTML。本章知识下，你会如何重构类型声明与创建方式（列 3 条 OOP 层面改法，不写具体框架）？

---

## 8. [面试]

`instanceof`  与向下转型（强制类型转换）分别在什么场景使用？不做  `instanceof`  直接  `(Dog) animal`  可能引发什么异常？

---

## 9. [面试]

什么是  **模板方法**？`AbstractReport.generate()`  固定头尾、子类实现  `exportBody()`  体现了什么好处？

---

## 10. [工作]

代码审查发现  `class MiniCar extends Engine`，仅为调用  `start()`  和  `getPower()`。从  **is-a 语义**  与  **继承带来的耦合**  两方面，说明这种写法的问题，并给出本章范围内更稳妥的思路（一句话方向即可）。
