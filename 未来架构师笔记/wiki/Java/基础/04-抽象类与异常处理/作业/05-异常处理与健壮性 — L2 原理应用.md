---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---

> 课程：Java 面向对象与核心类库 · 第 3 章  
> 说明：根据现象推理结论，答案写清步骤

---

## 1.

某方法签名：

```java
public void save(String path) throws IOException {
    // 内部 throw new IOException("磁盘满");
}
```

调用方  `main`  直接写  `save("a.txt");`  无法编译。请按  **现象 → 原因 → 修复**  说明，并给出一种让  `main`  通过编译的写法（伪代码或一行 try-catch 即可）。

---

## 2.

```java
try {
    int x = Integer.parseInt("20");
    int y = 10 / 0;
} catch (NumberFormatException e) {
    System.out.println("A");
} catch (RuntimeException e) {
    System.out.println("B");
} finally {
    System.out.println("C");
}
```

请说明控制台输出顺序（若某行不输出写「无」），并解释  **`ArithmeticException`**  被哪个 catch 捕获、**finally**  是否执行。

---

## 3.

`BankAccount`  中：

```java
public void withdraw(double amount) {
    if (amount > balance) {
        throw new InsufficientFundsException("余额不足");
    }
    balance -= amount;
}
```

`AccountDemo`  未 try-catch 直接调用  `account.withdraw(200);`。程序能否编译？运行若余额不足会怎样？请区分  **编译期**  与  **运行期**。

---

## 4.

同事写了：

```java
try {
    readConfig();
} catch (Exception e) {
    // 暂时忽略
}
```

线上配置错误被静默。请说明  **吞异常**  的风险（至少 2 点），并给出一种更稳妥的处理方向（一句话，不要求完整代码）。

---

## 5.

`OrderService.findById`  声明  `throws OrderNotFoundException`（受检）。`OrderDemo`  已正确 try-catch。若改为  `OrderNotFoundException extends RuntimeException`  且去掉  `throws`，`OrderDemo`  的 try-catch 在编译期是否仍必须？调用方不写 catch 时运行期行为有何变化？各用一句话。

---

## 6.

控制台栈片段：

```text
java.lang.NullPointerException
    at NpeDemo.printLength(NpeDemo.java:8)
    at NpeDemo.main(NpeDemo.java:15)
```

请说明应优先查看哪一行、该行可能哪个变量为  `null`，以及沿栈向上还应检查什么（一句话）。

---

## 7.

```java
Integer box = null;
int value = box;
```

会编译通过吗？运行时会抛什么？请用  **自动拆箱**  一句话解释。

---

## 8.

`FileReadHelper.readFirstLine`  使用  `try-with-resources`  读取  `demo.txt`。若文件不存在，会抛哪类异常？该异常属于受检还是非受检？调用方  `FileReadCaller`  为何必须 catch 或 throws？

---

## 9.

`AccountValidator.checkOwner`  先  `Objects.requireNonNull(owner, ...)`  再判断  `owner.trim().isEmpty()`。若传入  `owner = " "`（空格），会抛 NPE 还是  `IllegalArgumentException`？顺序为何不能颠倒（先  `isEmpty`  再  `requireNonNull`）？

---

## 10.

下列两处设计，哪处更符合本章「业务语义」建议？各用一句话说明。

- A. `findById`  找不到时  `return null`，调用方未判空导致 NPE
- B. `findById`  找不到时  `throw new OrderNotFoundException("未找到: " + orderId)`
