---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---
> 课程：Java 面向对象与核心类库 · 覆盖第 2 章全部 5 节  
> 说明：操作与定义题

---

## 1. 一、继承与方法重写

### 1.1 （填空）在 Java 中，子类声明继承父类使用 **\_\_** 关键字，例如 `class Dog extends Animal`。

### 1.2 （填空）子类提供与父类同名、同参数列表的方法覆盖父类实现，称为方法 **\_\_**，常用注解 **\_\_** 标记。

### 1.3 （判断）Java 一个类可以同时 `extends` 两个父类。（ ）

### 1.4 （选择）`Dog` 继承 `Animal`，父类 `eat()` 为 `public`，子类重写时下列合法的是：

- A. `private void eat()`
- B. `public void eat()`
- C. `public void eat(String food)`（仅多一个参数）
- D. 以上 B、C 都是重写

### 1.5 （填空）父类中被 `final` 修饰的方法，子类 **\_\_**（填「可以」或「不可以」）重写。

---

## 2. 二、多态与运行时绑定

### 2.1 （填空）`Shape s = new Circle(3.0);` 中，编译期类型是 **\_\_**，运行期类型是 **\_\_**。

### 2.2 （判断）上例中 `s.area()` 调用的是 `Circle` 类中重写的 `area()` 方法。（ ）

### 2.3 （选择）下列代码能否通过编译？

```java
Animal a = new Dog("旺财", "柯基");
a.bark();
```

- A. 能，运行期会调用 `Dog.bark()`
- B. 不能，`Animal` 类型没有 `bark()` 方法
- C. 能，会自动向下转型
- D. 不能，必须先 `(Dog) a`

### 2.4 （填空）向下转型前通常用 **\_\_** 判断引用指向的对象是否为某子类实例。

### 2.5 （填空）实例方法的多态分派发生在 **\_\_**（填「编译期」或「运行期」）。

---

## 3. 三、抽象类与模板方法

### 3.1 （填空）用 `______ class` 声明的类不能直接 `new` 实例化。

### 3.2 （判断）抽象方法可以有方法体，只要方法名与抽象类中声明一致即可。（ ）

### 3.3 （选择）`AbstractReport` 中 `generate()` 固定调用 `exportBody()`，子类 `PdfReport` 实现 `exportBody()`。这属于：

- A. 方法重载
- B. 模板方法模式
- C. 接口多实现
- D. 静态方法隐藏

### 3.4 （填空）具体子类 `PdfReport` 必须实现父类中声明的全部 **\_\_** 方法，否则应把子类也声明为抽象类。

### 3.5 （改错）`AbstractReport r = new AbstractReport("月报");` 无法编译，用一句话说明原因。

---

## 4. 四、接口与面向接口编程

### 4.1 （填空）类实现接口使用 **\_\_** 关键字，例如 `class AlipayPayment implements Payable`。

### 4.2 （填空）一个类最多 extends 一个类，但可以实现 **\_\_** 个接口。

### 4.3 （判断）`Payable p = new Payable();` 是创建接口实例的合法写法。（ ）

### 4.4 （选择）业务方法参数写成 `void checkout(Payable p, double amount)` 的主要好处是：

- A. 只能使用支付宝
- B. 调用方传入任意 `Payable` 实现，与具体支付类解耦
- C. 不需要实现 `pay` 方法
- D. 可以 extends 多个父类

### 4.5 （填空）接口 `Payable` 中的 `default void printReceipt()` 属于带默认实现的方法，实现类 **\_\_**（填「必须」或「可以不」）重写。

---

## 5. 五、组合与综合

### 5.1 （填空）`Car` 类中有成员 `private Engine engine;`，表达的是 **\_\_**（填 has-a 或 is-a）关系。

### 5.2 （判断）从设计语义上，`class Car extends Engine` 通常比 `Car` 持有 `Engine` 成员更合理。（ ）

### 5.3 （填空）`Car.start()` 内部调用 `engine.start()`，属于 **\_\_**（填「委托」或「重写」）。

### 5.4 （选择）下列设计更合理的是：

- A. `Dog extends Animal`（狗是一种动物）
- B. `Car extends Engine`（车是一种引擎）
- C. `Payable extends AlipayPayment`
- D. `Shape extends Circle`

### 5.5 （读代码）`Shape[] shapes = { new Circle(2), new Rectangle(3, 4) };` 循环中 `shape.area()` 每次调用是否可能进入不同类的 `area()` 实现？（填「是」或「否」，一句话说明依据。）

### 5.6 （填空）`AbstractReport pdf = new PdfReport("销售"); pdf.generate();` 体现了 **\_\_** 引用指向 **\_\_** 对象。（各填一类关键词）

### 5.7 （判断）父类的 `static` 方法被子类定义同名 static 方法，属于方法重写并参与多态。（ ）

### 5.8 （填空）`WechatPayment` 与 `AlipayPayment` 都实现 `Payable`，切换 `Payable payment = new ...` 的 `new` 右侧类名即可换渠道，这体现了 **\_\_** 接口编程。

### 5.9 （选择）`SportsCar` 需要更强动力，较合适的做法是：

- A. `class SportsCar extends Engine`
- B. `SportsCar` 继承 `Car`，构造时传入更大功率的 `Engine`
- C. `Engine extends SportsCar`
- D. 删除 `Engine`，把功率写死在 `SportsCar` 所有方法里

### 5.10 （填空）子类构造器中调用父类构造应使用 **\_\_**(...)，且须写在构造器 **\_\_**。
