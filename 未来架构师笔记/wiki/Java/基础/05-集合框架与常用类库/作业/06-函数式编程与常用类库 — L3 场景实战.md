---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---
> 课程：Java 面向对象与核心类库 · 第 5 章  
> 说明：面试题为主，少量工作场景

---

## 1. [面试]

什么是函数式接口？Lambda 表达式与匿名内部类相比主要简化了什么？举一个本章示例中的使用场景（类名或 API 名）。

---

## 2. [面试]

Stream 的「中间操作」与「终端操作」有何区别？为什么只调用 `filter` 和 `map` 可能看不到结果？

---

## 3. [面试]

`Optional.of`、`Optional.ofNullable`、`Optional.empty` 分别适用于什么场景？为什么不推荐滥用 `optional.get()`？

---

## 4. [面试]

`LocalDate`、`LocalTime`、`LocalDateTime` 各表示什么？`Period` 与 `Duration` 的使用场景有何不同？

---

## 5. [面试·扩展]

`parallelStream` 在什么情况下可能带来收益？初学阶段为什么建议默认使用串行 `stream()`？

---

## 6. [面试]

`try-with-resources` 解决了什么问题？读写中文文本文件时为什么要显式指定 `StandardCharsets.UTF_8`？

---

## 7. [工作]

接口返回 `List<Order>`，需要 **筛选状态为 PAID 的订单** 并 **提取订单号列表**。请用本章 Stream API 写出核心流水线步骤（类名 + 操作名链，不要求完整可编译代码）。

---

## 8. [面试]

`Comparator.comparing(Student::getName)` 中的 `Student::getName` 属于什么语法？与 `(a, b) -> a.getName().compareTo(b.getName())` 的关系是什么？

---

## 9. [面试]

`Files.readAllLines` 与 `BufferedReader` 按行读取各适合什么规模的文件？相对路径 `data/sample.log` 在 IDEA 中运行时通常相对哪个目录？

---

## 10. [工作]

`UserLookupService.findNicknameById` 返回 `Optional<String>`。调用方需要：有昵称则打印欢迎语，无昵称则使用「访客」。请写出应使用的 Optional API 名称（可写两种写法各一个 API）。
