---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---

> 课程：Java 面向对象与核心类库 · 覆盖第 4 章全部 5 节  
> 说明：操作与定义题

---

## 1. 一、List、Set、Map 结构选型

### 1.1 （填空）Java 单列集合的根接口是 **\_\_**，而键值对结构使用独立的 **\_\_** 接口。

### 1.2 （填空）`List`  允许元素 **\_\_**（填「重复」或「不重复」），且保持 **\_\_** 顺序。

### 1.3 （判断）`Map`  继承自  `Collection`  接口。（ ）

### 1.4 （选择）需要「用户 id → 登录 token」快速查找，应优先使用：

- A. `ArrayList`
- B. `HashSet`
- C. `HashMap`
- D. `LinkedList`

### 1.5 （填空）推荐写法：`List<String> names = new ______<>();`（填实现类名）。

---

## 2. 二、ArrayList 与 LinkedList

### 2.1 （填空）`ArrayList`  内部基于可扩容的 **\_\_** 实现；按下标  `get(i)`  访问通常为 O(1)。

### 2.2 （判断）无特殊理由时，业务代码中的通用  `List`  实现应优先选择  `LinkedList`。（ ）

### 2.3 （选择）`TaskQueue`  使用  `addLast`  与  `removeFirst`，更适合的实现类是：

- A. `ArrayList`
- B. `LinkedList`
- C. `HashMap`
- D. `HashSet`

### 2.4 （填空）`LinkedList`  按索引  `get(500)`  在大列表上通常比  `ArrayList` **\_\_**（填「慢」或「快」）。

### 2.5 （填空）`ProductCatalog`  使用  `List<String>`  字段，具体实现为 **\_\_**。

---

## 3. 三、HashMap 原理与冲突

### 3.1 （填空）`HashMap`  通过键的 **\_\_**() 方法计算桶位置，用 **\_\_**() 判断是否为同一键。

### 3.2 （判断）`HashMap`  允许存在多个不同的  `null`  键。（ ）

### 3.3 （选择）`scores.put("Alice", 88)`  后再  `put("Alice", 90)`，`get("Alice")`  返回：

- A. 88
- B. 90
- C. null
- D. 编译错误

### 3.4 （填空）无法区分「键不存在」与「值为 null」时，应使用  `map.______(key)`  方法。

### 3.5 （判断）自定义类作为  `HashMap`  的键时，可以只重写  `equals`  而不重写  `hashCode`。（ ）

---

## 4. 四、泛型类、泛型方法与通配符

### 4.1 （填空）`GenericBox<T>`  中的  `T`  称为 **\_\_** 参数；创建实例应写  `new GenericBox<______>()`。

### 4.2 （填空）`public static <T> void printList(List<T> list)`  中的  `<T>`  写在方法返回类型之前，表示这是一个 **\_\_** 方法。

### 4.3 （判断）`List<String>`  可以赋值给  `List<Object>`  以存放任意类型。（ ）

### 4.4 （选择）`sumNumbers(List<? extends Number> nums)`  中，`? extends Number`  主要表示：

- A. 只能写入 Number
- B. 可读 Number 及其子类元素，不宜随意 add
- C. 与 raw type 等价
- D. 运行期会保留完整泛型类型信息供反射

### 4.5 （填空）PECS 口诀中，Producer Extends, Consumer **\_\_**（填 Super 或 super 均可）。

---

## 5. 五、集合遍历、排序与去重

### 5.1 （填空）增强 for 写法：`for (String s : ______)`  中，冒号右侧必须是可迭代集合。

### 5.2 （判断）在增强 for 循环中直接调用  `list.remove(0)`  删除当前元素，是推荐做法。（ ）

### 5.3 （填空）在遍历中安全删除元素，应使用 **\_\_** 的  `remove()`  方法。

### 5.4 （填空）`Student`  实现  `Comparable<Student>`  后，对  `List<Student>`  自然排序可调用  `Collections.______(students)`。

### 5.5 （选择）对  `List<Integer>`  去重且不要求保持原顺序，简便做法是：

- A. `new ArrayList<>(new HashSet<>(list))`
- B. `list.sort(null)`
- C. `map.put`  每个整数
- D. 双重 for 删除

### 5.6 （填空）遍历  `Map`  同时获取键和值，应使用  `map.______()`  的 for-each。

### 5.7 （判断）`HashSet`  去重后，元素顺序一定与原始  `List`  完全一致。（ ）

### 5.8 （填空）在  `Comparator`  的  `compare`  方法中，返回 0 表示两个元素 **\_\_**（填「相等」或「排序相同」均可）。

### 5.9 （读代码）`CollectionIterateDemo`  用  `Iterator`  删除空字符串，避免的是 **\_\_**ModificationException（填完整类名前缀  `Concurrent`）。

### 5.10 （选择）按姓名对  `List<Student>`  排序且学号排序规则不变，应：

- A. 修改  `hashCode`
- B. 使用  `Collections.sort`  并传入  `Comparator<Student>`
- C. 把  `Student`  改成  `Map`
- D. 删除所有泛型
