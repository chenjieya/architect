---
author: human
ai_editable: false
updated_by: human
updated: 2026-08-02
---
> 课程：Java 面向对象与核心类库 · 第 4 章  
> 说明：面试题为主，少量工作场景

---

## 1. [面试]

`List`、`Set`、`Map` 在语义上各解决什么问题？各举一个本章示例类或场景名。

---

## 2. [面试]

`ArrayList` 与 `LinkedList` 的底层结构有何不同？为什么多数业务默认 `ArrayList`？

---

## 3. [面试]

`HashMap` 的 `put` 过程如何用 `hashCode` 与 `equals`？什么是哈希冲突？JDK 8+ 桶内过长时大致如何处理（一句话）？

---

## 4. [面试]

Java 泛型的主要作用是什么？什么是类型擦除（初学版一句话即可）？

---

## 5. [面试·扩展]

`? extends T` 与 `? super T` 分别适合「往外取」还是「往里放」？PECS 口诀如何记？

---

## 6. [面试]

`Comparable` 与 `Comparator` 的区别是什么？`Collections.sort(list)` 与 `Collections.sort(list, comparator)` 分别依赖哪一种？

---

## 7. [工作]

接口返回 `List<Order>`，调用方需要 **按订单号去重** 且 **保留首次出现顺序**。请写出本章推荐的数据结构组合思路（类名 + 步骤，不要求完整代码）。

---

## 8. [面试]

为什么不能在增强 for 中直接 `list.remove()`？应使用什么 API？

---

## 9. [面试]

`Map.get(key)` 返回 `null` 时，如何判断是键不存在还是值为 null？

---

## 10. [工作]

线上日志：`NullPointerException at ReportService.export(ReportService.java:41)`，对应代码为 `cache.get(userId).getName()`，`cache` 为 `HashMap<Long, UserProfile>`。请从 **Map 使用** 与 **空值防御** 两方面各列 1 条排查或改进建议（不写 Spring，不超本章）。
