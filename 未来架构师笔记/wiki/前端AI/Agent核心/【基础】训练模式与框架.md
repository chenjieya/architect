---
author: ai
ai_editable: true
summary: '训练模式与框架：梯度下降的三种批量模式（SGD 全批量 小批量）、训练效果四概念（欠拟合 过拟合 泛化 灾难性遗忘）、CNN 与 Transformer 两大网络架构、PyTorch 等深度学习框架，以及模型文件（权重+配置）的构成。'
refs:
  pages:
    - 【基础】梯度下降
    - 【基础】反向传播
    - 【基础】前向传播
    - 深度学习
    - 【LLM】Transformer
    - 【导言】AI岗位分布与学习路线
  raw:
    - path: raw/agent-core/06. 训练模式和框架/课件.md
      sha256: 5bbb50ba5cc0cc01028680e4d10854ff2b2ba314a169a1ad71b7e8399875534d
updated_by: ai
updated: 2026-08-22
---

前面几讲把训练的"一次循环"讲完了：[[【基础】前向传播]] 算结果、[[【基础】反向传播]] 算梯度、[[【基础】梯度下降]] 改参数。这一讲回答三个更大问题：**一次改参数要用几个样本？用什么架构的网络？用什么工具、训练完留下什么？** 这就是"训练模式和框架"。

## 1. 一句话理解

**训练模式 = 一次用多少样本算梯度；网络架构 = 模型长什么样；框架 = 用什么工具把训练跑起来；模型文件 = 训练完留下的成果。**

## 2. 梯度下降的三种训练模式

[[【基础】梯度下降]] 讲的是"一个参数怎么改"，但每次该**拿几个样本**去算梯度？这个数量决定了训练模式，主要有三种：

![mnist_100_digits](https://picgo-1300696809.cos.ap-beijing.myqcloud.com/obsidian/1787389695362_202607161711415.png)

### 2.1 随机梯度下降（SGD）：每次只看 1 个样本

- 每一步**只取 1 个样本**算梯度、更新参数。
- 特点：速度快（不用等所有样本算完），但**梯度噪声极大、收敛震荡**——单个样本的梯度方向往往很"偏"，会来回晃。
- 场景：早年小数据集常用，现代大模型基本不用。

> 大白话：SGD 像"只看一眼就决定方向"，反应快但容易走弯路。

### 2.2 全批量梯度下降（Full Batch）：每轮用全部样本

- 每一轮更新参数：**使用全部训练集的所有样本**计算损失与梯度，再更新权重。
- 特点：梯度方向**最稳定**（综合了所有样本的信息）；但大数据集下**显存/内存爆炸、迭代极慢**。
- 场景：几乎不用于大模型训练。

> 大白话：全批量像"把整本书看完才决定方向"，方向最准但太慢，数据一多就跑不动。

### 2.3 小批量梯度下降（Mini-Batch）：大模型的主流

- 每一步取**固定数量样本**（`batch_size`，如 32 / 128 / 1024）计算梯度更新。
- 特点：**平衡稳定性与速度**，显存可控，梯度方向介于前两者之间。
- 场景：**LLM、CV 模型全部默认使用**，是当前训练的事实标准。

> 大白话：小批量像"一次看一小段再决定方向"，兼顾了准头和速度，是工程上的最优解。

**三者对比**：

| 模式              | 每次用几个样本        | 梯度稳定度   | 速度 | 适用           |
| ----------------- | --------------------- | ------------ | ---- | -------------- |
| SGD 随机          | 1 个                  | 差（噪声大） | 快   | 早年小数据集   |
| 全批量 Full Batch | 全部                  | 最稳         | 极慢 | 几乎不用       |
| 小批量 Mini-Batch | 固定数量（如 32/128） | 好           | 快   | **大模型主流** |

## 3. 训练效果：欠拟合、过拟合与泛化

学会"怎么训练"之后，还有个更要紧的问题：**训练出来的模型到底学得好不好？** 判断训练效果，先要分清三组概念。

### 3.1 欠拟合（Underfitting）：还没学会

模型太简单、或者训练得不够久，连**训练集本身**都没学好。

- 表现：在训练集上表现就差（损失降不下来）。
- 原因：模型容量不够（如层数太少）、特征没喂进去、训练轮数不足。
- 大白话：像学生**上课没听懂**，做作业就做不对——别说考试（新数据），连课本例题（训练数据）都不会。

> 特征：训练损失高，测试损失也高，两者都不好。

### 3.2 过拟合（Overfitting）：死记硬背

模型"聪明过头"，把训练集**背下来了**，而不是学到背后的规律。

- 表现：在训练集上成绩极好（几乎零误差），但一换新数据就露馅。
- 原因：模型容量太大、参数太多、训练太久、数据太少。
- 大白话：像学生**死记硬背答案**，作业全对，但考题稍微一变就不会了——它背的是"答案本身"，不是"解题方法"。

> 特征：训练损失很低，但测试损失高——两者的**差距拉大**就是过拟合的警报。

### 3.3 泛化（Generalization）：真正要衡量的本事

上面两个概念，其实都是从**一个**核心能力出发的：**模型在没见过的数据（测试集）上的表现**，就叫**泛化能力**。

- 欠拟合 = 泛化差（因为没学好）。
- 过拟合 = 泛化也差（因为只背住了训练集）。
- **训练的真正目标**：既不是训练集成绩，也不是测试集成绩，而是**尽量好的泛化能力**——既别没学会，也别死记硬背。

> 大白话：**泛化 = 学到的规律能不能用到新场合**。训练集成绩只是"在校表现"，泛化能力才是"毕业后混社会的能力"。

> 一句话记忆：**欠拟合 = 没学会；过拟合 = 背答案；泛化 = 真本事**。三者关系：欠拟合和过拟合是"泛化差"的两个方向——一个是学得不够，一个是学过头。

### 3.4 灾难性遗忘（Catastrophic Forgetting）：学会新东西，忘光旧东西

在真实训练中，模型往往不是一次训完，而是**边学边喂新数据**（比如先学会 A 任务，再学 B 任务）。这时会出现一个反直觉的现象：**模型在学习新任务 B 时，会把之前学会的任务 A 忘得精光**——这就是**灾难性遗忘**。

- 为什么？因为神经网络的参数是**全体共享**的：学 B 任务要调整的权重，恰好也是记住 A 任务的权重，改动了它们，A 的记忆就没了。
- 大白话：像硬盘被"写覆盖"——不是 A 被删了，而是 A 的信息被 B 的新数据**冲掉了**。
- 典型场景：微调（fine-tuning）一个通用模型去做特定任务时，模型会"丢掉"原本的通用能力；增量学习、持续学习中尤其严重。

> 反直觉点：我们总以为"新知识不会覆盖旧知识"，但神经网络恰恰会。这是为什么大模型微调要**谨慎、加约束**（如用少量旧样本混合训练、冻结部分层）的原因——具体对策后续微调章节再展开。

### 3.5 一图看懂四个概念的关系

| 概念       | 一句话   | 训练集表现     | 新数据表现 | 本质       |
| ---------- | -------- | -------------- | ---------- | ---------- |
| 欠拟合     | 没学会   | 差             | 差         | 学得不够   |
| 过拟合     | 背答案   | 极好           | 差         | 学过头了   |
| 泛化能力   | 真本事   | ——             | 好才是王道 | 核心目标   |
| 灾难性遗忘 | 学新忘旧 | 旧任务突然变差 | ——         | 参数被覆盖 |

> 大白话总括：**欠拟合和过拟合是"泛化差"的两个极端，灾难性遗忘是"继续学新东西"时特有的坑。** 训练的艺术，就是在"别没学会"和"别学过头"之间，找到泛化能力最好的那个点。

## 4. 网络架构：模型长什么样

数据喂进去之后，靠什么结构去"理解"它？两大经典架构：

### 4.1 CNN（卷积神经网络）：图像领域经典

**模仿人眼视觉**：图像是**局部关联**的，不需要一次性看整张图，只用滑动小窗口（**卷积核**）去提取局部纹理、边缘、色块，再层层组合成高层特征。

- 代表：图像分类、目标检测等 CV 任务。
- 详细讲解见 [[深度学习]]。

### 4.2 Transformer：NLP 起家，现在是通用全能架构

**用注意力机制**处理序列，能并行计算、捕获长距离依赖，几乎取代了 RNN。今天的大语言模型（ChatGPT 等）全部基于它。

- 本系列后续第 7～12 讲会详细展开（词元、词嵌入、注意力机制、Transformer 完整训练流程）。
- 快速了解可先看 [[【LLM】Transformer]]。

> 一句话：**图像先想到 CNN，语言/通用大模型想到 Transformer**。完整架构谱系见 [[深度学习]]。

## 5. 深度学习框架：用什么工具训练

写神经网络不该从零造轮子，用现成的深度学习框架即可：

- **PyTorch**：**现在的主力训练框架**，生态丰富、写起来灵活，学术界和工业界大模型训练几乎都用它。
- **TensorFlow**：曾经的主流，**现在已经不再流行**，通常只用于一些边缘计算场景。

> 大白话：框架就像"装修队的全套工具"——别人已经把"搭网络、算梯度、跑 GPU"这些最累的活都封装好了，你只管设计网络和准备数据。

## 6. 模型：训练完留下什么

训练结束后的"成果"叫**模型**，通常以**多个文件**的形式存储，包含：

- **核心权重文件**：占模型体积约 **99%**。模型"学到的东西"全在这里——几十亿个数字（权重）。
- **配置文件**：记录模型的**网络架构、隐藏层维度、权重精度**等信息。有了配置 + 权重，才能完整还原一个能用的模型。

> 大白话：**权重文件是"大脑"，配置文件是"说明书"**——两者缺一不可。这也是为什么大模型发布时通常给出一组文件而不是单个文件。

## 7. 总结

- **训练模式**：SGD（1 样本，噪声大）→ 全批量（全部样本，太慢）→ **小批量（大模型主流）**。
- **训练效果**：欠拟合（没学会）、过拟合（背答案）、泛化（真本事）、灾难性遗忘（学新忘旧）。
- **架构**：CNN 管图像，Transformer 管语言/通用（后续详讲）。
- **框架**：PyTorch 是主力，TensorFlow 已边缘化。
- **模型文件** = 权重文件（99%）+ 配置文件。

这一讲结束，"AI 的本质"这块地基就铺完了（第 1～6 讲）。下一讲开始进入"语言模型怎么理解文字"：从词元（Token）讲起——分词原理见 [[【基础】词元]]，概念全景见 [[【LLM】词元]]；完整路线见 [[【导言】AI岗位分布与学习路线]]。
