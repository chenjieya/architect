# Agent 核心实现逻辑分析

## 一、整体架构

本项目实现了一个**基于 ReAct 模式的 AI Agent**，采用模块化设计，各组件职责分明。核心架构分为以下几层：

```
┌─────────────────────────────────────┐
│           Agent (入口/编排)           │
├────────┬────────┬────────┬──────────┤
│ Model  │Session │  Tool  │ Listener │
│(模型层)│(会话层) │(工具层) │ (监听层)  │
├────────┴────────┴────────┴──────────┤
│          EventEmitter (事件总线)       │
└─────────────────────────────────────┘
```

---

## 二、核心模块详解

### 1. `agent.py` — Agent 主控（编排层）

**职责**：作为整个 Agent 的入口，组织 ReAct 循环的执行流程。

**关键逻辑**：

```python
class Agent:
    def invoke(self, q: str):
        self.session.add_message({"role": "user", "content": q})
        responses = []
        while True:                          # ReAct Loop
            resp = self.model.invoke_stream(self.session)
            call_msg = registry.invoke(self.session.messages[-1])
            if call_msg is None:             # 没有 tool_calls → 结束
                responses.append(resp)
                break
            else:                            # 有工具调用 → 执行工具，继续循环
                self.session.messages += call_msg
                responses.append(resp)
        return responses
```

**ReAct 循环流程**：
1. 用户输入 → 加入 `session.messages`
2. 调用大模型（流式） → 得到响应消息
3. 检查响应中是否包含 `tool_calls`
   - **无** `tool_calls` → 结束循环，返回结果
   - **有** `tool_calls` → 执行工具，将结果加入会话，回到步骤 2

---

### 2. `model.py` — 模型层（与大模型交互）

**职责**：封装 OpenAI API 调用，支持流式（Stream）和非流式响应，并通过事件发射器（`EventEmitter`）向外广播推理过程。

**核心类**：

- **`Model`**（继承 `EventEmitter`）：管理与大模型的通信
  - `_invoke()` → 调用 OpenAI `chat.completions.create`
  - `_process()` → 处理响应结果
    - 非流式响应：直接归一化为 `InvokeResult`
    - 流式响应：通过三个 `Builder` 分别解析**推理内容、文本内容、工具调用**，边解析边发射事件

- **三个 Builder（构建器）**：
  | 构建器 | 解析字段 | 发射的事件 |
  |--------|----------|-----------|
  | `ReasoningBuilder` | `reasoning_content` | `reasoning_start / reasoning / reasoning_end` |
  | `ContentBuilder` | `content` | `content_start / content / content_end` |
  | `ToolCallsBuilder` | `tool_calls` | `tool_call_start / tool_call / tool_call_end` |

- **`InvokeResult`**：封装模型返回的消息（`message: dict`）和原始数据（`raw`）

---

### 3. `session.py` — 会话层（管理上下文）

**职责**：维护完整的对话上下文（消息历史 + 工具定义），支持持久化。

**关键功能**：

| 方法/属性 | 说明 |
|-----------|------|
| `messages` | 消息列表（system / user / assistant / tool） |
| `tools` | 工具的 Schema 列表，传给 OpenAI 用 |
| `add_message()` | 添加一条消息到历史 |
| `save()` | 将当前会话保存为 JSON 文件到 `sessions/` 目录 |
| `load()` | 从 JSON 文件恢复会话 |
| `print_friendly()` | 以人类可读格式打印整个对话历史 |

**初始化自动加载**：通过 `render_prompt("system")` 渲染 Jinja2 系统提示词模板，将**当前工作目录、操作系统、是否 Git 仓库**等环境信息注入到 system prompt 中。

---

### 4. `tool/` — 工具层（Function Calling 实现）

#### 4.1 `tool/core/tool.py` — Tool 类与注册机制

**`Tool` 类**：将 Python 函数包装为 LLM 可识别的工具。

- `schema()` → 生成 OpenAI 兼容的工具定义 JSON Schema
- `__call__()` → 执行工具函数，**异常安全**（捕获异常并返回堆栈信息）
- 参数验证：利用 Pydantic 动态创建参数模型，自动校验类型和必填项

**`@tool` 装饰器**：通过 `param_descriptions` 为参数添加描述信息。

#### 4.2 `tool/core/utils.py` — 参数模型工厂

**`create_params_model()`**：利用 `inspect` 模块解析函数签名，结合 `pydantic.create_model` 动态生成参数校验模型，支持：
- 类型注解推导
- 必填/可选参数识别
- 参数描述注入

#### 4.3 `tool/__init__.py` — 工具注册中心

**`_ToolRegistry` 类**：全局唯一的工具注册器。

```python
class _ToolRegistry:
    register(tool)      # 注册工具
    schemas()           # 返回所有工具的 Schema 列表
    invoke(message)     # 从模型消息中提取 tool_calls 并逐一执行
```

**内置三个工具**：
| 工具 | 函数 | 说明 |
|------|------|------|
| `read_file` | `read_file(filepath)` | 读取文件内容 |
| `write_file` | `write_file(filepath, content)` | 写入内容到文件 |
| `bash_command` | `bash_command(command)` | 执行 Shell 命令 |

---

### 5. `events.py` — 事件系统（观察者模式）

**`Event` 类**：定义事件常量，包括推理事件、内容事件、工具调用事件和完成事件。

**`EventEmitter` 类**：实现发布-订阅模式。

| 方法 | 说明 |
|------|------|
| `on(event, callback)` | 注册事件监听器 |
| `emit(event, **data)` | 发射事件，触发所有监听器 |

`Model` 继承自 `EventEmitter`，在流式推理过程中发射事件，供监听器消费。

---

### 6. `listener.py` — 监听器抽象基类

**`ModelListener`**（抽象类）：定义监听模型的接口。

- `listen(model)` → 将自身注册到 Model 的所有事件上
- `_handler(e, **args)` → 子类需实现的抽象方法，处理具体事件

---

### 7. `printer.py` — 监听器实现（控制台打印）

**`ModelPrinterListener`**：继承 `ModelListener`，将模型推理过程实时打印到终端。

| 事件 | 打印内容 |
|------|----------|
| `reasoning_start` | "思考中..." |
| `reasoning_end` | 换行 |
| `content_start` | "内容：" |
| `content` | 绿色文本输出内容片段 |
| `content_end` | 换行 |
| `tool_call_end` | 青色显示 "调用工具：xxx" |

---

### 8. `config.py` — 配置管理

使用 `pydantic-settings` 从 `.env` 文件或环境变量读取 OpenAI 配置：

| 配置项 | 环境变量前缀 | 说明 |
|--------|-------------|------|
| `api_key` | `OPENAI_API_KEY` | API 密钥 |
| `base_url` | `OPENAI_BASE_URL` | API 端点 |
| `model` | `OPENAI_MODEL` | 模型名称 |

---

### 9. `prompt.py` — 提示词模板引擎

使用 **Jinja2** 模板引擎渲染系统提示词。

`render_prompt("system")` 将三个环境变量注入模板：
- `{{ cwd }}` → 当前工作目录
- `{{ os }}` → 操作系统
- `{{ is_git }}` → 是否在 Git 仓库内

**模板文件** `prompt/system.j2` 描述了 AI 助手的基本行为和环境信息。

---

## 三、数据流全链路

```
用户输入 "读取 xxx 文件"
    │
    ▼
Agent.invoke(q)
    │
    ├─ session.add_message({"role":"user", "content": q})
    │
    ├─ 进入 ReAct Loop
    │   │
    │   ├─ Model.invoke_stream(session)
    │   │   │
    │   │   ├─ OpenAI API 调用（stream=True）
    │   │   │
    │   │   ├─ 流式解析（3个Builder）
    │   │   │   ├─ ReasoningBuilder → 发射 reasoning_* 事件
    │   │   │   ├─ ContentBuilder  → 发射 content_* 事件
    │   │   │   └─ ToolCallsBuilder → 发射 tool_call_* 事件
    │   │   │
    │   │   ├─ ModelPrinterListener 实时打印到控制台
    │   │   │
    │   │   └─ 返回 InvokeResult (含 message)
    │   │
    │   ├─ registry.invoke(message)
    │   │   │
    │   │   ├─ 解析 tool_calls
    │   │   ├─ 逐一执行工具（read_file / write_file / bash_command）
    │   │   └─ 返回 tool 角色的结果消息列表
    │   │
    │   ├─ 有 tool_calls？ → 结果加入 session，继续循环
    │   └─ 无 tool_calls？ → 结束循环
    │
    └─ 返回所有响应
```

---

## 四、设计亮点

1. **ReAct 模式**：通过"思考→行动→观察"循环，让 Agent 能自主调用工具解决问题
2. **事件驱动架构**：`EventEmitter` + `ModelListener` 解耦了模型推理与副作用（打印、日志等）
3. **流式处理**：三个 Builder 分别并行解析推理过程、内容输出和工具调用，实时性强
4. **工具即函数**：通过 `@tool` 装饰器 + Pydantic 动态建模，将普通 Python 函数无缝转换为 LLM 可识别的工具
5. **会话持久化**：支持保存/恢复对话历史，便于调试和继续对话
6. **环境感知**：通过 Jinja2 模板将运行时环境信息自动注入到 system prompt 中
