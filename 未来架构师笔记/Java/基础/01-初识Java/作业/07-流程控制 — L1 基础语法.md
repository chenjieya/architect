> 课程：Java 语言基础入门 · 覆盖第 3 章全部 5 节  
> 说明：操作与定义题

---

## 1. 一、if/else 多分支

### 1.1 （填空）Java 的 `if` 条件表达式结果类型必须是 **\_\_**。

### 1.2 （判断）`if (x = 1)` 在 Java 中是合法的条件写法。（ ）

### 1.3 （选择）下列关于 `else if` 链的说法，正确的是：

- A. 所有分支都会执行一遍
- B. 从上到下匹配，命中一条后不再判断后续分支
- C. 必须把 `else` 写在第一个
- D. 条件可以是任意 int 值

### 1.4 （填空）判断「90 分及以上优秀、60 分及以上及格」时，应先判断 **\_\_**（填「>=90」或「>=60」）的分支。

### 1.5 （判断）`if` 代码块 `{ }` 内声明的变量，可以在块外继续使用。（ ）

---

## 2. 二、switch

### 2.1 （填空）传统 `switch` 写法中，在一个 `case` 末尾通常需要写 **\_\_**，以防止执行流进入下一个 case。

### 2.2 （选择）JDK 17 中，箭头写法 `case 1 -> ...` 的特点是：

- A. 必须手动 break
- B. 默认不会 fall-through 到下一 case
- C. 不能用于 String
- D. 不能写 default

### 2.3 （填空）`switch` 的 `case` 标签必须是 **\_\_** 期可确定的常量，不能是普通变量。

### 2.4 （判断）分数区间「0～59 不及格、60～100 及格」适合用 switch 实现。（ ）

### 2.5 （选择）下列更适合用 switch 的场景是：

- A. `score >= 60 && score < 90`
- B. 变量 `day` 等于 1～7 表示星期几
- C. BMI 是否在 18.5～24
- D. 年龄是否大于 18 且小于 60

---

## 3. 三、for、while、do-while

### 3.1 （填空）`for` 循环的三个部分依次是：**\*\***、**\*\***、**\_\_**（用中文或 init/条件/更新 均可）。

### 3.2 （选择）三种循环中，**至少执行一次**循环体的是：

- A. for
- B. while
- C. do-while
- D. 三种都一样

### 3.3 （判断）`do { ... } while (条件)` 中，`while (条件)` 后面的分号可以省略。（ ）

### 3.4 （读代码）执行后共打印几行 `i = ...`？（填数字）

```java
for (int i = 0; i < 3; i++) {
    System.out.println("i = " + i);
}
```

### 3.5 （填空）`for (int i = 0; i < 5; i++)` 中声明的 `i`，在循环外能否使用？**\_\_**（能/不能）。

---

## 4. 四、break 与 continue

### 4.1 （填空）`break` 用于结束当前 **\_\_** 或最内层循环；`continue` 用于跳过本轮、进入 **\_\_**。

### 4.2 （读代码）下列循环打印的 `i` 值为（多选）：

```java
for (int i = 0; i < 5; i++) {
    if (i == 2) {
        continue;
    }
    System.out.println(i);
}
```

- A. 0
- B. 1
- C. 2
- D. 3
- E. 4

### 4.3 （判断）`continue` 可以用在传统 `switch` 的 `case` 分支中。（ ）

### 4.4 （填空）带标签的 `break outer;` 用于从 **\_\_** 循环中直接跳出。

---

## 5. 五、嵌套循环

### 5.1 （填空）外层循环 5 次、内层每次完整执行 4 次时，内层循环体大约执行 **\_\_** 次（填乘积）。

### 5.2 （判断）嵌套循环中，内外层应使用不同的循环变量名（如 `i` 与 `j`）。（ ）

### 5.3 （读代码）`NestedLoopDemo` 中 `rows=3`、`cols=4` 时，共打印 **\_\_** 行，每行 **\_\_** 个 `*`（不含空格）。

### 5.4 （选择）在嵌套循环中已找到目标，希望外层循环也立即停止，应：

- A. 只在内层写 `break;`
- B. 使用 `break outer;`（outer 为外层标签）或等效标志
- C. 使用 `continue;`
- D. 删除内层循环

---

> 参考答案见 [[03-流程控制 — 参考答案]]
