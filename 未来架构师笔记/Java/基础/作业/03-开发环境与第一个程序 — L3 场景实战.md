> L3 以**常规面试题**为主，可含基于本章知识点的**适当扩展**；场景题仅使用本章已讲概念（JDK、Path、IDEA、javac/java 等）

---

## 1. [面试] JDK、JRE、JVM 的区别与关系

**问**：简述 JDK、JRE、JVM 各自是什么、三者包含关系如何？开发 Java 程序应安装哪一个？

---

## 2. [面试] Java 程序从源码到运行的过程

**问**：

1. 请描述 `.java` 文件如何变成可运行结果（涉及 `javac`、`JVM`）。
2. `javac` 与 **JIT** 分别发生在什么阶段？二者作用有何不同？

---

## 3. [面试] main 方法的签名

**问**：为什么 Java 程序入口必须是 `public static void main(String[] args)`？请分别说明 `public`、`static`、`String[] args` 的作用。

---

## 4. [面试·扩展] 编译型语言与跨平台

**问**：常有人说 Java 是「编译型语言」，Python 是「解释型语言」。结合本章所学，这种二分法是否足够准确？「一次编译，到处运行」具体指什么在跨平台？

---

## 5. [工作] 终端与 IDEA 表现不一致

**场景**：某开发者用 Cursor 终端执行 `java -version` 显示 17；同一台电脑打开 IDEA 导入项目后，代码中 `String` 报红（`Cannot resolve symbol`），Run 也无法通过。团队要求统一 JDK 17（Temurin）。

**问**：

1. 可能原因有哪些？（区分系统 Path 与 IDEA Project SDK）
2. 列出排查与修复步骤。
3. 系统学习 Java 后端时，IDEA 与 Cursor 分别适合承担什么角色？（简要）

---

> 参考答案见 [[01-开发环境与第一个程序 — 参考答案]]
