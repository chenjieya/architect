> 课程：Java 面向对象与核心类库 · 覆盖第 1 章全部 4 节  
> 说明：操作与定义题

---

## 1. 一、类、对象与属性行为

### 1.1 （填空）在 Java 中，描述一类事物模板的是 **\_\_**，通过 **\_\_** 关键字创建类的具体实例。

### 1.2 （判断）定义了 `class Student` 之后，不执行 `new Student()` 也可以直接调用实例方法 `study()`。（ ）

### 1.3 （选择）下列关于 `Student s1 = new Student(); Student s2 = s1;` 的说法，正确的是：

- A. `s2` 是新创建的对象，与 `s1` 数据互不影响
- B. `s1` 与 `s2` 引用同一对象，修改其一的成员会影响另一个
- C. `s1 == s2` 为 `false`
- D. 必须写 `new Student(s1)` 才能赋值

### 1.4 （填空）定义在类中、描述对象状态的变量称为 **\_\_**；描述对象行为的函数称为 **\_\_**。

### 1.5 （填空）通过对象引用访问成员使用 **\_\_** 运算符，例如 `s1.setName("张三")`。

---

## 2. 二、封装与访问权限

### 2.1 （填空）把对象内部数据隐藏起来、对外提供受控接口的原则称为 **\_\_**。

### 2.2 （选择）希望字段只能在本类内部访问，应使用修饰符：

- A. `public`
- B. `private`
- C. `protected`
- D. 不写修饰符且跨包访问

### 2.3 （判断）`BankAccount` 中 `balance` 为 `private` 时，类外部的 `main` 方法可以通过 `acc.balance = 100` 直接修改余额。（ ）

### 2.4 （填空）读取私有字段 `owner` 的常用方法名是 **\_\_**；写入时常用 **\_\_**（按 JavaBean 习惯填写方法名前缀）。

### 2.5 （改错）`BankAccount` 的 `deposit` 方法应拒绝非正金额。下列调用后余额仍被加上负数，问题出在哪一类设计？

```java
acc.deposit(-100);
```

（用一句话说明应把规则写在何处。）

---

## 3. 三、构造器与初始化

### 3.1 （填空）与类名相同、没有返回值类型的特殊方法是 **\_\_**。

### 3.2 （判断）类中只定义了 `public Book(String title, double price)` 时，仍可以无条件使用 `new Book()`。（ ）

### 3.3 （选择）`Book` 类同时存在 `public Book()` 与 `public Book(String title, double price)`，这属于：

- A. 方法重写
- B. 构造器重载
- C. 方法覆盖
- D. 接口实现

### 3.4 （填空）创建对象时，成员变量先赋 **\_\_**，再执行实例初始化块，最后执行 **\_\_** 方法体。

### 3.5 （读代码）`InitOrderDemo` 无参构造创建对象时，控制台前两行输出依次是什么？（按学习文档示例类名作答，写出关键字即可。）

---

## 4. 四、this 与 super

### 4.1 （填空）在实例方法中，`this` 表示 **\_\_** 对象的引用。

### 4.2 （填空）在一个构造器内调用本类另一个构造器，应使用 **\_\_**，且必须写在构造器 **\_\_**。

### 4.3 （判断）`Employee` 继承 `Person` 时，子类构造器可以在任意一行调用 `super(name)`。（ ）

### 4.4 （选择）`Employee` 类中 `public Employee(String name, String dept)` 第一行写 `super(name);` 的主要作用是：

- A. 调用本类的无参构造
- B. 调用父类 `Person` 的构造，初始化继承来的 `name`
- C. 创建一个新的 `Person` 对象返回
- D. 声明 `Employee` 实现接口

### 4.5 （填空）子类重写 `introduce` 时，先执行父类版本再打印部门，应使用 **\_\_** 调用父类方法。

---

## 5. 五、综合

### 5.1 （选择）下列哪项是合法的类与对象使用？

- A. `Student.study();` 且 `study` 为非 static 实例方法，未创建对象
- B. `Student s = new Student(); s.study();`
- C. `new Student = s;`
- D. `class s = new Student();`

### 5.2 （填空）`public class Employee extends Person` 中，`extends` 表示 **\_\_** 关系。

### 5.3 （判断）`static` 方法内可以使用 `this` 访问当前对象。（ ）

### 5.4 （填空）`Book` 无参构造里写 `this("未命名", 0.0);`，表示无参构造通过 **\_\_** 委托给两参构造完成字段赋值。

### 5.5 （读代码）执行后输出几行？第一行内容关键词是什么？（类名 `Employee`、`Person` 按学习文档）

```java
Employee e = new Employee("赵六", "研发");
e.introduce();
```

（填空：输出 **\_\_** 行；第一行包含「我是」与姓名。）
