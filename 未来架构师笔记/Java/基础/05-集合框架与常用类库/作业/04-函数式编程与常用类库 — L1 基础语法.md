> 课程：Java 面向对象与核心类库 · 覆盖第 5 章全部 5 节  
> 说明：操作与定义题

---

## 1. 一、Lambda 表达式语法与函数式接口

### 1.1 （填空）Lambda 表达式的基本形式是 `(参数列表) -> ______` 或代码块。

### 1.2 （填空）函数式接口要求有且仅有 **\_\_** 个抽象方法。

### 1.3 （判断）Lambda 表达式可以独立存在，不需要函数式接口类型。（ ）

### 1.4 （选择）将 `List<Student>` 按姓名排序，推荐写法是：

- A. `Collections.sort(students, new Comparator<Student>() { ... })` 仅此一种
- B. `Comparator.comparing(Student::getName)`
- C. `students.lambda(Student::getName)`
- D. `Stream.sort(Student::getName)`

### 1.5 （填空）`Predicate<String>` 的抽象方法返回类型是 **\_\_**。

---

## 2. 二、Stream 流水线处理与性能考量

### 2.1 （填空）从 `List<Order>` 创建 Stream 的写法是 `orders.______()`。

### 2.2 （判断）`filter` 和 `map` 属于终端操作，调用后立即执行完整流水线。（ ）

### 2.3 （选择）`StreamPipelineDemo` 中收集结果为 `List` 应使用：

- A. `toArray()`
- B. `collect(Collectors.toList())`
- C. `forEach(Collectors.toList())`
- D. `reduce(Collectors.toList())`

### 2.4 （填空）只有调用了 **\_\_** 操作（如 `collect`、`count`），Stream 流水线才会真正执行。

### 2.5 （判断）数据量很小时，应优先使用 `parallelStream()` 以获得更好性能。（ ）

---

## 3. 三、Optional 空值处理模型

### 3.1 （填空）可能为 null 的值应使用 `Optional.______(value)` 创建 Optional。

### 3.2 （判断）`Optional.of(null)` 会得到空的 Optional 且不抛异常。（ ）

### 3.3 （选择）`UserLookupService.findNicknameById(99L)` 无对应用户时，安全的默认昵称获取方式是：

- A. `optional.get()`
- B. `optional.orElse("访客")`
- C. `optional.of("访客")`
- D. `Optional.getOrNull()`

### 3.4 （填空）无值时才执行昂贵默认值计算，应使用 `orElse______` 方法。

### 3.5 （判断）推荐在类的每个字段上都使用 `Optional` 类型。（ ）

---

## 4. 四、LocalDateTime 与时间计算实践

### 4.1 （填空）仅包含年月日的类型是 `______`。

### 4.2 （填空）会员有效期加 12 个月，应使用 `startDate.plus(Period.of______(12))`。

### 4.3 （选择）会议开始时间加 2 小时，应使用：

- A. `start.plus(Period.ofHours(2))`
- B. `start.plus(Duration.ofHours(2))`
- C. `start.addHours(2)`
- D. `LocalTime.ofHours(2)`

### 4.4 （填空）格式化 `LocalDateTime` 常用 `DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:______")`（填秒的模式字母）。

### 4.5 （判断）新代码应优先使用 `java.util.Date` 处理业务日期。（ ）

---

## 5. 五、文件 IO 读写与资源管理

### 5.1 （填空）将字符串路径转为 `Path` 可使用 `Paths.______(...)`。

### 5.2 （填空）`FileIODemo` 写入文本文件时应指定字符集 `StandardCharsets.______`。

### 5.3 （选择）按行读取较大日志文件，更适合使用：

- A. `Files.readAllLines` 一次性读入
- B. `BufferedReader` 的 `readLine()` 循环
- C. `FileInputStream.read()` 单字节循环
- D. `System.in`

### 5.4 （填空）自动关闭 `BufferedReader` 的推荐语法是 **\_\_**-with-resources。

### 5.5 （判断）`Files.readAllLines` 读取不存在的文件时可能抛出 `IOException` 或其子类。（ ）
