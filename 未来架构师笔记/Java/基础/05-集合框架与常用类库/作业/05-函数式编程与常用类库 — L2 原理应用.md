> 课程：Java 面向对象与核心类库 · 第 5 章  
> 说明：根据现象推理结论，答案写清步骤

---

## 1.

某同学写了以下代码但控制台无任何业务输出：

```java
List<Order> orders = Arrays.asList(
        new Order("O001", 120.0, "PAID"),
        new Order("O002", 80.0, "CANCELLED")
);
orders.stream()
        .filter(o -> "PAID".equals(o.getStatus()))
        .map(Order::getAmount);
```

请按 **现象 → Stream 阶段 → 结论** 说明原因，并写出应补充的**终端操作**名称（写一个即可）。

---

## 2.

`ComparatorLambdaDemo` 中三种排序写法结果相同。请说明 **Lambda 表达式** 必须依赖什么类型的接口，以及该接口的核心特征（一句话）。

---

## 3.

`UserLookupService` 中 `nicknameById.get(id)` 可能返回 null。方法改为返回 `Optional<String>` 并使用 `Optional.ofNullable`。请说明 **`ofNullable` 与 `of`** 在传入 null 时的行为差异（各一句话）。

---

## 4.

某日志时间字符串按模式 `yyyy-MM-dd HH:mm:ss` 解析失败，抛出 `DateTimeParseException`。实际字符串为 `2026/06/05 14:30:00`。请说明 **原因** 及应调整的 **DateTimeFormatter 模式**（写出正确 pattern 字符串）。

---

## 5.

Windows 环境下 `FileIODemo` 写入中文「应用启动」后，用记事本打开 `data/sample.log` 出现乱码，但 IDEA 控制台打印正常。请从 **读写是否指定同一字符集** 角度说明可能原因与修复方式（提及应用的标准字符集常量名）。

---

## 6.

```java
List<Order> orders = new ArrayList<>();
orders.stream()
        .filter(o -> o.getAmount() > 100)
        .forEach(orders::add);
```

请说明此写法在逻辑上有什么问题（不少于 2 句），并说明应用 **`collect`** 得到新列表的正确思路（不要求完整代码）。

---

## 7.

`BookingTimeDemo` 中 `memberStart.plus(Period.ofMonths(12))` 与 `start.plus(Duration.ofHours(2))` 分别解决哪类时间计算？各举本节一个字段或场景词（如「会员到期」「会议结束」）。

---

## 8.

某程序对仅含 50 个元素的 `List` 使用 `parallelStream().filter(...).collect(Collectors.toList())`，耗时反而比 `stream()` 更长。请从 **线程开销与数据规模** 角度给出 **现象 → 推理 → 结论**（不少于 3 步）。

---

## 9.

`OptionalDemo` 中对 empty 的 Optional 调用 `orElse("访客")` 与 `orElseGet(() -> expensiveDefault())`：当 Optional **有值** 时，`expensiveDefault()` 是否一定会执行？请分别说明两种方法在有值、无值时对默认值计算的行为差异。

---

## 10.

`LogFileReader` 使用 `try (BufferedReader reader = Files.newBufferedReader(path, StandardCharsets.UTF_8))`。若不用 try-with-resources 而在 `finally` 中手动 `close()`，请说明 try-with-resources 在 **发生异常时关闭资源** 方面带来的主要好处（一句话）。
