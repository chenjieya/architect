> 课程：Java 面向对象与核心类库 · 第 4 章  
> 说明：根据现象推理结论，答案写清步骤

---

## 1.

某同学用 `List<String> tags = new ArrayList<>();` 保存用户标签，代码中双重循环判断 `contains` 后再 `add`，仍担心漏删。请说明应改用哪种 **集合接口** 及 **实现类**（各写一名称），并一句话说明原因。

---

## 2.

`ListSetMapDemo` 中 `Set` 两次 `add("A")` 后 `size()` 为 1，而 `List` 两次 `add("A")` 后 `size()` 为 2。请从 **是否允许重复** 角度解释，并说明若业务需要「唯一用户名列表」应选 List 还是 Set。

---

## 3.

某程序对含 10 万元素的 `ArrayList` 执行 1 万次 `get(50000)`，又对同等规模的 `LinkedList` 执行同样操作。请按 **现象 → 内部结构 → 结论** 说明为何通常 `ArrayList` 明显更快。

---

## 4.

`HashMapDemo` 中 `scores.get("英语")` 返回 `null`。可能有两种情况。请列出这两种情况，并说明如何用 **`containsKey`** 区分「键不存在」。

---

## 5.

自定义类 `UserKey { int id; String name; }` 只重写了 `equals`（比较 id），未重写 `hashCode`。向 `HashMap<UserKey, String>` 放入两个 `new UserKey(1,"a")` 与 `new UserKey(1,"b")` 后，`size()` 可能为 2。请说明 **hashCode 与 equals 在 HashMap 中的分工** 及此现象原因（不少于 2 句）。

---

## 6.

```java
List<? extends Number> nums = new ArrayList<Integer>();
nums.add(1);          // 编译错误
Number n = nums.get(0);
```

请说明为何 **add 编译失败** 而 **get 可以赋给 Number**（用泛型不变性 / 上界通配符各一句话）。

---

## 7.

```java
List<String> list = new ArrayList<>();
list.add("a");
list.add("b");
for (String s : list) {
    if (s.isEmpty()) {
        list.remove(s);
    }
}
```

运行可能抛 `ConcurrentModificationException`。请说明 **触发原因**，并给出一种本章推荐的改法（写「使用 Iterator.remove」或等价描述即可）。

---

## 8.

`SortDedupDemo` 先按学号 `Collections.sort(students)`，再传入按姓名比较的 `Comparator` 排序。若 `compare` 对姓名返回 0，排序后两名同姓同学的相对顺序可能怎样？（一句话，提到「稳定」或「相等元素」即可。）

---

## 9.

`List<Integer> raw = Arrays.asList(3, 1, 2, 1, 3);`  
`List<Integer> distinct = new ArrayList<>(new HashSet<>(raw));`

请说明 **去重原理**（Set 哪条规则），并说明若业务要求 **保持首次出现顺序** 应把 `HashSet` 换成什么（本章文档提到的类名）。

---

## 10.

`UserSessionMap.getToken(userId)` 调用方写：

```java
String token = sessionMap.getToken(100L);
System.out.println(token.length());
```

当用户未登录、`get` 返回 `null` 时会 NPE。请结合第 3 章习惯，给出两种改进方向各一句话（如 `containsKey`/判空、或明确文档约定），不要求写框架代码。
