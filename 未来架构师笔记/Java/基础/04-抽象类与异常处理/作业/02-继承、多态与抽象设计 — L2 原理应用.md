> 课程：Java 面向对象与核心类库 · 第 2 章  
> 说明：根据现象推理结论，答案写清步骤

---

## 1.

某同事写了：

```java
public class Dog extends Animal {
    public void eat(String food) {
        System.out.println("吃 " + food);
    }
}
```

父类 `Animal` 已有 `public void eat()`。调用 `Dog d = new Dog("旺财", "柯基"); d.eat();` 时，输出仍是父类「正在吃东西」，而不是带 `food` 的版本。请按 **现象 → 原因 → 修复** 说明，并写出一种正确重写父类 `eat()` 的写法（方法签名一行即可）。

---

## 2.

```java
Shape s = new Circle(5.0);
System.out.println(s.area());
```

已知 `Circle` 重写了 `area()`。有同学说「因为 `s` 声明为 `Shape`，所以一定执行 `Shape.area()` 返回 0」。请反驳：说明 **编译期类型** 与 **运行期类型** 分别是什么，以及实际调用哪个方法。

---

## 3.

```java
Animal a = new Cat("咪咪");
System.out.println(a instanceof Dog);
System.out.println(a instanceof Cat);
```

两行分别输出什么？若之后写 `Dog d = (Dog) a;`，会发生什么？请分 **编译期 / 运行期** 说明。

---

## 4.

`AbstractReport` 有 `public final void generate()` 和 `protected abstract void exportBody();`。同事在 `TextReport` 中重写：

```java
@Override
public void generate() {
    exportBody();
}
```

无法通过编译。请说明 **`final` 的作用** 与 **模板方法设计意图**（各一句话），并指出 `TextReport` 应改哪一类方法。

---

## 5.

`PaymentDemo` 中：

```java
Payable p = new AlipayPayment();
p.pay(100);
p = new WechatPayment();
p.pay(50);
```

第二次赋值后，`checkout(Payable p, double amount)` 若只依赖参数 `p`，是否需要修改方法签名或方法体才能支持微信？为什么？

---

## 6.

`Car` 定义为：

```java
public class Car {
    private Engine engine;
    public Car(String model, int power) {
        this.engine = new Engine(power);
    }
    public void start() {
        engine.start();
    }
}
```

若改为 `public class Car extends Engine`，语义上会出现什么问题？从 **is-a / has-a** 各用一句话说明。

---

## 7.

父类 `Animal` 有 `public static void info()`，子类 `Dog` 也定义 `public static void info()`。调用：

```java
Animal a = new Dog("x", "y");
a.info();
```

与 `Dog.info()` 相比，多态是否发生？`Animal.info()` 与 `Dog.info()` 的关系应称 **重写** 还是 **隐藏**？

---

## 8.

`Shape[] shapes = { new Circle(1), new Rectangle(2, 3) };`  
循环中仅写 `shape.draw()`，不写 `if (shape instanceof Circle)`。为何仍能打印不同内容？若新增 `Triangle extends Shape` 并重写 `draw()`，循环代码是否需要修改？说明 **开闭原则** 直觉（一句话）。

---

## 9.

`AlipayPayment` 同时 `implements Payable, Loggable`。若 `Payable` 新增抽象方法 `void refund(double amount)`，而 `AlipayPayment` 未补实现，编译结果如何？这与 **接口契约** 有什么关系？

---

## 10.

子类 `PdfReport` 构造器忘记写 `super(title)`，父类 `AbstractReport` 只有 `public AbstractReport(String title)`。编译报错提示父类构造器不可见。请写出 **修复后的 `PdfReport` 构造器前两行**（参数仍为 `String title`）。
