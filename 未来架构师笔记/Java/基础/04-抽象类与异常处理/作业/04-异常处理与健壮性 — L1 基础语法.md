> 课程：Java 面向对象与核心类库 · 覆盖第 3 章全部 5 节  
> 说明：操作与定义题

---

## 1. 一、Throwable 体系与异常分类

### 1.1 （填空）Java 中所有异常和错误的根类是 **\_\_**。

### 1.2 （填空）继承 `Exception` 且不是 `RuntimeException` 子类的异常，称为 **\_\_** 异常（填中文或英文 Checked 均可）。

### 1.3 （判断）`NullPointerException` 属于受检异常，编译器强制调用方必须 try-catch。（ ）

### 1.4 （选择）下列属于非受检（Unchecked）异常的是：

- A. `IOException`
- B. `OrderNotFoundException`（若定义为 `extends Exception`）
- C. `IllegalArgumentException`
- D. 以上都是受检

### 1.5 （填空）查看异常发生位置时，应重点阅读调用栈中第一个出现在 **\_\_** 包/项目下的 `at 类名.方法(文件:行号)` 行。

---

## 2. 二、try-catch-finally

### 2.1 （填空）多个 catch 块时，应将更 **\_\_**（填「具体」或「笼统」）的异常类型写在前面。

### 2.2 （判断）`finally` 块在 `try` 中发生 return 之后通常仍会执行。（ ）

### 2.3 （选择）`try (BufferedReader reader = new BufferedReader(new FileReader(path)))` 属于：

- A. 多 catch 语法
- B. try-with-resources 自动关闭资源
- C. throws 声明
- D. 自定义异常

### 2.4 （填空）`catch` 块中什么都不写、也不重新抛出的做法称为 **\_\_** 异常（填「吞掉」或「吞没」均可）。

### 2.5 （改错）下列 catch 顺序为何无法编译？用一句话说明原因。

```java
try { /* ... */ }
catch (Exception e) { }
catch (IOException e) { }
```

---

## 3. 三、throw 与 throws

### 3.1 （填空）在方法体内主动抛出异常对象使用 **\_\_** 关键字；在方法签名上声明可能抛出的受检异常使用 **\_\_** 关键字。

### 3.2 （判断）`public void withdraw(double amount) throws IllegalArgumentException` 表示调用方编译期必须 catch `IllegalArgumentException`。（ ）

### 3.3 （选择）`FileReadHelper.readFirstLine` 签名含 `throws IOException`，调用方合法做法是：

- A. 必须 try-catch `IOException` 或继续 throws
- B. 可以不处理，编译器不检查
- C. 只能 throw，不能 catch
- D. 把 IOException 改成 RuntimeException 才能编译

### 3.4 （填空）`BankAccount.withdraw` 中余额不足时写 `throw new IllegalArgumentException(...)`，属于在方法内 **\_\_**（填「抛出」）异常。

### 3.5 （读代码）若 `load()` 内 `throw new IOException(...)` 且未在 `load` 内 catch，`load` 方法签名至少应如何声明？（写 `throws` 一行即可）

---

## 4. 四、自定义业务异常

### 4.1 （填空）`InsufficientFundsException` 通常继承 **\_\_**（填 `Exception` 或 `RuntimeException`，以本章非受检示例为准）。

### 4.2 （填空）`OrderNotFoundException extends Exception` 时，`OrderService.findById` 应声明 **\_\_**，调用方须 try-catch 或继续 throws。

### 4.3 （判断）自定义异常类名建议以 `Error` 结尾以便识别。（ ）

### 4.4 （选择）下列消息设计更利于排查的是：

- A. `throw new InsufficientFundsException();`（无消息）
- B. `throw new InsufficientFundsException("账户 张三 余额不足，需要 200，当前 100");`
- C. `throw new NullPointerException();` 表示订单不存在
- D. B 和 C 都合理

### 4.5 （填空）子类异常构造器中把说明文字传给父类，应写 **\_\_**(message)（填 `super` 或 `this`）。

---

## 5. 五、NPE 与边界异常

### 5.1 （填空）对 `null` 引用调用 `length()` 等方法会抛出 **\_\_** 异常。

### 5.2 （填空）`Integer.parseInt("12a")` 可能抛出 **\_\_**；访问 `arr[10]` 而数组长度为 3 可能抛出 **\_\_**。

### 5.3 （判断）用 `return null` 表示「订单不存在」，让调用方触发 NPE，是推荐做法。（ ）

### 5.4 （填空）`Objects.requireNonNull(username, "用户名不能为 null")` 在 `username` 为 null 时会抛出 **\_\_**。

### 5.5 （选择）`AccountValidator.checkOwner` 在构造 `BankAccount` 前调用，主要体现：

- A. 多态
- B. 防御式编程 / 入口校验
- C. 模板方法
- D. 接口多实现

### 5.6 （填空）排查 NPE 时，除异常类名外，应结合调用栈找到第一个 **\_\_** 项目中的源文件行号。

### 5.7 （判断）`Objects.requireNonNull` 可以同时防止 null 和空字符串 `""`。（ ）

### 5.8 （读代码）`printLength(null)` 在修复前会在哪一类语句抛异常？（填方法调用或字段访问等一句话）

### 5.9 （填空）`BoundaryDemo.parseIndex` 在下标非法时主动 `throw new IllegalArgumentException`，属于 **\_\_** 失败而不是等数组越界。（填「提前」或「早」均可）

### 5.10 （选择）业务上「订单号不存在」较合适的做法是：

- A. `return null` 且不文档说明
- B. `throw new OrderNotFoundException("未找到订单: " + orderId)`
- C. 不处理，让 JVM 随机崩溃
- D. `throw new Error("无订单")`
