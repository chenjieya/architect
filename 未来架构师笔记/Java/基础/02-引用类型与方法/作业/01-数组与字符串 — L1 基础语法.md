> 课程：Java 语言基础入门 · 覆盖第 4 章全部 5 节及堆栈/定长/拼接要点  
> 说明：操作与定义题

---

## 1. 一、一维数组

### 1.1 （填空）Java 一维数组合法的下标范围是 **\_\_** 到 **\_\_**。

### 1.2 （判断）`int[] arr = new int[5];` 创建后，5 个元素的初始值均为 0。（ ）

### 1.3 （选择）获取数组元素个数应使用：

- A. `arr.length()`
- B. `arr.length`
- C. `arr.size()`
- D. `arr.count`

### 1.4 （填空）增强 for 循环的写法是：`for (元素类型 ______ : 数组名)`。

### 1.5 （读代码）下列代码输出几行 `arr[i] = ...`？（填数字）

```java
int[] arr = {10, 20};
for (int i = 0; i < arr.length; i++) {
    System.out.println("arr[" + i + "] = " + arr[i]);
}
```

### 1.6 （判断）`int[] a = {1, 2, 3}; int[] b = a;` 之后修改 `b[0] = 9`，`a[0]` 的值也会变为 9。（ ）

### 1.7 （判断）数组创建后可以通过 `arr.length = 10;` 把长度改为 10。（ ）

### 1.8 （选择）`int[] arr = {1, 2, 3};` 需要再存两个元素时，本课推荐的做法是：

- A. `arr.length = 5;`
- B. `new` 一个更长的数组，拷贝旧元素，再让 `arr` 指向新数组
- C. 在原数组末尾自动增加两格
- D. 使用 `ArrayList.add`（本课未要求掌握其 API）

### 1.9 （判断）`int[] arr = {1, 2, 3};` 是合法的数组字面量初始化。（ ）

### 1.10 （判断）`int[] arr = new int[3]{1, 2, 3};` 是合法的写法。（ ）

---

## 2. 二、二维数组

### 2.1 （填空）对于 `int[][] matrix`，`matrix.length` 表示数组的 **\_\_** 数；`matrix[i].length` 表示第 i **\_\_** 的元素个数。

### 2.2 （选择）遍历可能每行列数不同的二维数组时，内层循环边界应写：

- A. `j < matrix[0].length`
- B. `j < matrix[i].length`
- C. `j < matrix.length`
- D. `j <= matrix[i].length`

### 2.3 （判断）`int[][] a = new int[2][];` 之后可以直接使用 `a[0][0] = 1` 而不会出错。（ ）

### 2.4 （读代码）`int[][] m = {{1,2},{3,4,5}};` 中 `m[1].length` 的值是 **\_\_**。

---

## 3. 三、String 不可变与比较

### 3.1 （填空）比较两个 `String` 的**字符内容**是否相同，应使用 **\_\_** 方法。

### 3.2 （判断）`String s = "Hi"; s = s + "!";` 执行后，原来 `"Hi"` 对象的内容被原地修改为 `"Hi!"`。（ ）

### 3.3 （选择）下列代码中，用于判断**字符内容**相同的是：

```java
String a = "Java";
String b = new String("Java");
```

- A. `a == b`
- B. `a.equals(b)`
- C. `a.length() == b.length()`
- D. `a = b`

### 3.4 （读代码）下列代码输出是 true 还是 false？（填 true / false）

```java
String s1 = "Hello";
String s2 = "Hello";
System.out.println(s1 == s2);
```

### 3.5 （填空）`String s3 = new String("Hello");` 与字面量 `"Hello"` 比较内容应写 `s3.______("Hello")`；比较是否为同一对象才用 \` == \`。

### 3.6 （填空）避免 `username` 为 `null` 时调用 `username.equals("admin")` 崩溃，可写成 **\_\_**.equals(username)（填常量示例）。

### 3.7 （判断）`""` 与 `null` 表示同一个含义。（ ）

---

## 4. 四、StringBuilder

### 4.1 （判断）使用 `StringBuilder` 后，`String` 类型就变成可变的了。（ ）

### 4.2 （填空）`StringBuilder` 在循环中 `append` 完毕后，应调用 **\_\_** 方法得到 `String` 类型结果。

### 4.3 （选择）在循环内把 `int[] arr = {1,2,3,4,5}` 拼成 `"1,2,3,4,5"`，更合适的做法是：

- A. `String s = "";` 循环里 `s = s + arr[i];`
- B. `StringBuilder` + 循环 `append`，最后 `toString()`
- C. 用 \` == \` 连接各元素
- D. 修改 `String` 内部字符

### 4.4 （判断）`sb.append("a").append("b")` 是合法的链式写法。（ ）

### 4.5 （读代码）下列程序输出的一行内容是？（照抄输出格式）

```java
int[] arr = {1, 2, 3, 4, 5};
StringBuilder sb = new StringBuilder();
for (int i = 0; i < arr.length; i++) {
    if (i > 0) {
        sb.append(",");
    }
    sb.append(arr[i]);
}
System.out.println(sb.toString());
```

---

## 5. 五、常用字符串方法与边界

### 5.1 （填空）`"Hello".substring(1, 4)` 的结果是 **\_\_**。

### 5.2 （填空）`"abc".indexOf("z")` 的返回值是 **\_\_**。

### 5.3 （选择）`" ".trim().isEmpty()` 的结果是：

- A. true
- B. false
- C. 编译错误
- D. 运行期异常

### 5.4 （判断）`substring(begin, end)` 包含下标 `end` 处的字符。（ ）

### 5.5 （填空）`String line = "a,b,c"; String[] parts = line.split(",");` 之后 `parts.length` 为 **\_\_**。

### 5.6 （选择）校验用户名字符串时，合理的**第一步**是：

- A. 直接 `name.charAt(0)`
- B. 判断 `name == null`
- C. 直接 `name.substring(0, 3)`
- D. 用 `name == ""`

### 5.7 （选择）判断字符串是否包含子串 `"error"`，应优先使用：

- A. `text == "error"`
- B. `text.contains("error")`
- C. `text.length("error")`
- D. `text.substring("error")`

---

> 参考答案见 [[01-数组与字符串 — 参考答案]]
