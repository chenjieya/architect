[project]
name = "fastapi-learn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.5"
dependencies = []

[tool.uv.workspace]
members = [
    "apps/*",
]

[dependency-groups]
dev = [
    "debugpy>=1.8.21",
]
